function overview(){const dates=planDates().slice(0,state.quietWeekends===false?5:7),team=planPeople(),ids=team.map(p=>p.id),issues=people.filter(p=>p.issue&&!p.ack).length;return `${state.quietTasks===false?'':`<div class="cq-tasks"><span>Zu erledigen</span>${btn('allrequests',`<strong>${pending()}</strong> Anträge`,'cq-task')}${btn('priorityIssues',`<strong>${issues}</strong> Zeitprüfungen`,'cq-task')}</div>`}<section class="cq-calendar" aria-label="Wochenübersicht der Abwesenheiten"><div class="cq-calendar-head"><div><h2>Diese Woche</h2><span>${dates[0].toLocaleDateString('de-CH',{day:'numeric',month:'long'})} – ${dates[dates.length-1].toLocaleDateString('de-CH',{day:'numeric',month:'long',year:'numeric'})}</span></div><div class="cq-calendar-actions"><select data-plan-team aria-label="Team auswählen"><option value="">Alle Teams</option>${[...new Set(people.map(p=>p.team))].map(t=>`<option ${state.planTeam===t?'selected':''}>${E(t)}</option>`).join('')}</select>${state.week!==0?btn('plan-today','Heute','cq-text'):''}${btn('plan-prev',icon('chevron-left'),'cp-icon','aria-label="Vorige Woche"')}${btn('plan-next',icon('chevron-right'),'cp-icon','aria-label="Nächste Woche"')}</div></div><div class="cq-week" style="--cq-days:${dates.length}">${dates.map((date,index)=>{const iso=ymd(date),es=events.filter(e=>ids.includes(e.p)&&iso>=e.start&&iso<=e.end);return `<section class="cq-day ${iso==='2026-09-14'?'cq-today':''} ${index>4?'cq-weekend':''}"><button type="button" class="cq-day-heading" data-act="quiet-day" data-date="${iso}" aria-label="${date.toLocaleDateString('de-CH',{weekday:'long',day:'numeric',month:'long'})} öffnen"><span>${date.toLocaleDateString('de-CH',{weekday:'short'})}</span><strong>${date.getDate()}</strong></button><div class="cq-day-events">${es.map(e=>`<button type="button" class="cq-event ${e.kind==='Krank'?'cq-event-sick':''}" data-act="eventedit" data-id="${e.id}" aria-label="${people[e.p].name}: ${planKind(e)} bearbeiten"><span>${people[e.p].name}</span><small>${planKind(e)}${e.half?' · ½':''}</small></button>`).join('')||`<span class="cq-empty" aria-label="Keine Abwesenheiten">–</span>`}</div></section>`}).join('')}</div><div class="cq-calendar-foot"><span>Urlaub & Abwesenheiten</span>${btn('plan-month','Monatskalender '+icon('arrow-up-right'),'cq-text')}</div></section><footer class="cq-footer">Entwurf · Beispieldaten</footer>`}
function render(){renderBase();const title={overview:'Übersicht',time:'Zeitprüfung',requests:'Anträge',calendar:'Kalender',modules:'Module'}[state.view];q('.cp-page-head').innerHTML=`<div><h1>${title}</h1>${readOnly()?'<small>Nur Ansicht</small>':''}</div>${state.view==='overview'?btn('plan-new',icon('plus')+'Abwesenheit','cp-primary'):''}`;const bottom=q('.cp-side-bottom');if(bottom)bottom.innerHTML=`${btn('stamp',icon(state.stamped?'log-out':'log-in')+(state.stamped?'Ausstempeln':'Einstempeln'),'cq-text')}${btn('quiet-more',icon('settings-2')+'Mehr','cq-text')}`;icons()}
function quietMore(){open('Weitere Funktionen',`<div class="cq-more"><section><h3>Arbeiten</h3>${btn('quiet-workspaces',icon('panels-top-left')+'Arbeitsbereiche')}${btn('commands',icon('search')+'Suchen & Befehle')}${btn('stamp',icon('clock-3')+(state.stamped?'Ausstempeln':'Einstempeln'))}${btn('print',icon('printer')+'Zeiten drucken')}${btn('quiet-balances',icon('chart-no-axes-combined')+'Salden & Auswertung')}${btn('notifications',icon('bell')+'Benachrichtigungen')}</section><section><h3>Ansicht & Konto</h3>${btn('customize',icon('sliders-horizontal')+'Ansicht anpassen')}${btn('platform',icon('grid-2x2')+'Plattform & Module')}${btn('theme',icon('sun-moon')+'Hell / Dunkel')}${btn('language',icon('languages')+'Sprache wechseln')}${btn('account',icon('user-round')+'Konto & Einstellungen')}</section></div>`)}
function customization(){open('Ansicht anpassen',`<div class="cq-settings"><label class="cp-check"><input type="checkbox" data-quiet-setting="quietTasks" ${state.quietTasks!==false?'checked':''}>Offene Aufgaben auf der Übersicht anzeigen</label><label class="cp-check"><input type="checkbox" data-quiet-setting="quietWeekends" ${state.quietWeekends!==false?'checked':''}>Samstag und Sonntag im Wochenkalender anzeigen</label></div><div class="cp-sheet-foot">${btn('quiet-reset','Zurücksetzen')}${btn('customdone','Fertig','cp-primary')}</div>`)}
r.addEventListener('click',e=>{const b=e.target.closest('button[data-act^="quiet-"]');if(!b)return;e.preventDefault();e.stopImmediatePropagation();if(b.dataset.act==='quiet-day')return planDayDetail(b.dataset.date);if(b.dataset.act==='quiet-more')return quietMore();if(b.dataset.act==='quiet-workspaces'){const wasOpen=r.dataset.workspaces==='open';close();r.dataset.workspaces=wasOpen?'closed':'open';return}if(b.dataset.act==='quiet-reset'){state.quietTasks=true;state.quietWeekends=true;return customization()}if(b.dataset.act==='quiet-balances'){const avg=Math.round(people.reduce((sum,p)=>sum+p.balance,0)/people.length),top=[...people].sort((a,b)=>b.balance-a.balance)[0];open('Salden & Auswertung',`<div class="cq-balance-list"><p><span>Ø Überstundensaldo</span><strong>${money(avg)}</strong></p><p><span>Personen unter Soll</span><strong>${people.filter(p=>p.balance<0).length}</strong></p><p><span>Höchster Saldo · ${top.name}</span><strong>${money(top.balance)}</strong></p></div><div class="cp-sheet-foot">${btn('alltime','Zeitprüfung')}${btn('module','Analytics öffnen','cp-primary','data-module="Analytics"')}</div>`)}},true);
r.addEventListener('change',e=>{if(e.target.dataset.quietSetting)state[e.target.dataset.quietSetting]=e.target.checked});
