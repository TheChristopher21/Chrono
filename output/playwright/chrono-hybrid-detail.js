function planCompact(slot){if(slot.kind==='sick')return slot.text.startsWith('½')?'½ K':'K';if(slot.kind==='away')return slot.text.startsWith('½')?'½ U':slot.text==='Überstundenfrei'?'ÜF':'U';if(slot.kind==='warning')return '!';if(slot.kind==='off')return '–';return slot.text}
function planDayDetail(date){const team=planPeople(),ids=team.map(p=>p.id),es=events.filter(e=>ids.includes(e.p)&&date>=e.start&&date<=e.end);open('Tagesagenda · '+fmt(date),`<p class="cp-subtitle">${E(state.planTeam||'Alle Teams')}</p>${es.map(e=>`<div class="cp-row">${avatar(e.p)}<div class="cp-grow"><strong>${people[e.p].name}</strong><small>${planKind(e)} · ${fmt(e.start)} – ${fmt(e.end)}${e.half?' · halbtags':''}</small></div>${btn('eventedit','Bearbeiten','',`data-id="${e.id}"`)}</div>`).join('')||'<p class="cp-muted" style="margin-top:18px">Keine Abwesenheiten im ausgewählten Team.</p>'}<div class="cp-sheet-foot">${btn('sickdate','Krank melden','',`data-date="${date}"`)}${btn('vacationdate','+ Urlaub eintragen','cp-primary',`data-date="${date}"`)}</div>`)}
