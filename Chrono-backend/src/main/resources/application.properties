# ? MySQL Datenbank-Konfiguration mit Fehlerbehebung
spring.datasource.url=jdbc:mysql://localhost:3306/chrono_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=715841
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ? JWT Konfiguration
jwt.secret=MeinSuperGeheimerSchluessel1234567890
jwt.expiration=86400000  # 1 Tag in Millisekunden

# ? JPA und Hibernate Einstellungen
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ? Logging für bessere Fehleranalyse
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
