spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

app.initialize.admin=${APP_INITIALIZE_ADMIN:true}
app.admin.username=${APP_ADMIN_USERNAME:admin}
app.admin.password=${APP_ADMIN_PASSWORD:admin}

jwt.secret=MeinSuperGeheimerSchluessel1234567890
jwt.expiration=86400000
spring.jpa.open-in-view=true
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update
spring.jackson.time-zone=Europe/Zurich

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=OFF


spring.mail.host=${SPRING_MAIL_HOST}
spring.mail.port=${SPRING_MAIL_PORT}
spring.mail.username=${SPRING_MAIL_USERNAME:siefertchristopher@chrono-logisch.ch}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_SMTP_STARTTLS_ENABLE:true}

spring.security.user.name=test
spring.security.user.password=test
nfc.agent.token=SUPER-SECRET-AGENT-TOKEN

# Stripe
stripe.secret-key=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

# Allow larger multipart uploads (e.g., company logo files)
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB


# Local LLM endpoint
llm.base-url=${LLM_BASE_URL:http://localhost:5000}

# Banking integrations
banking.integrations.payments.enabled=false
banking.integrations.payments.endpoint=
banking.integrations.payments.api-key=
banking.integrations.signatures.enabled=false
banking.integrations.signatures.base-url=
banking.integrations.signatures.api-key=
banking.integrations.signatures.poll-interval=PT5M
banking.integrations.messages.enabled=false
banking.integrations.messages.endpoint=
banking.integrations.messages.api-key=

# Monitoring endpoints
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.prometheus.enabled=true
