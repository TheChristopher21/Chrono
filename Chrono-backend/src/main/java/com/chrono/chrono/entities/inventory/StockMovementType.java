package com.chrono.chrono.entities.inventory;

public enum StockMovementType {
    RECEIPT,
    ISSUE,
    TRANSFER,
    ADJUSTMENT,
    WRITE_OFF,
    GAIN,
    PRODUCTION_CONSUMPTION,
    PRODUCTION_OUTPUT
}
