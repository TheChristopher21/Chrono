diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/controller/CorrectionRequestController.java b/Chrono-backend/src/main/java/com/chrono/chrono/controller/CorrectionRequestController.java index de441dd6..b186d91d 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/controller/CorrectionRequestController.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/controller/CorrectionRequestController.java @@ -207,7 +207,7 @@ public class CorrectionRequestController {              Principal principal      ) {           try { -            CorrectionRequest deniedRequest = correctionRequestService.denyRequest(id, comment); +            CorrectionRequest deniedRequest = correctionRequestService.denyRequest(id, comment, principal.getName());              return ResponseEntity.ok(deniedRequest);          } catch (SecurityException e) {              logger.warn("Sicherheitsverletzung beim Ablehnen des Korrekturantrags ID {} durch {}: {}", id, principal.getName(), e.getMessage()); diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/controller/CustomerController.java b/Chrono-backend/src/main/java/com/chrono/chrono/controller/CustomerController.java index 51d803fd..be683292 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/controller/CustomerController.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/controller/CustomerController.java @@ -12,6 +12,7 @@ import org.springframework.web.bind.annotation.*;    import java.security.Principal;  import java.util.List; +import java.util.Map;  import java.util.Objects;    @RestController @@ -145,7 +146,7 @@ public class CustomerController {       * GEÄNDERT: Fügt eine Sicherheitsprüfung vor dem Löschen hinzu.       */      @DeleteMapping("/{id}") -    public ResponseEntity<Void> delete(@PathVariable Long id, Principal principal) { +    public ResponseEntity<?> delete(@PathVariable Long id, Principal principal) {          User user = getPrincipalUser(principal);          if (!featureEnabled(user)) {              return ResponseEntity.status(HttpStatus.FORBIDDEN).build(); @@ -157,9 +158,14 @@ public class CustomerController {                      if (!Objects.equals(customer.getCompany().getId(), user.getCompany().getId())) {                          return ResponseEntity.status(HttpStatus.FORBIDDEN).<Void>build();                      } -                    customerService.deleteById(id); -                    return ResponseEntity.noContent().<Void>build(); +                    try { +                        customerService.deleteById(id); +                        return ResponseEntity.noContent().<Void>build(); +                    } catch (IllegalStateException e) { +                        return ResponseEntity.status(HttpStatus.CONFLICT) +                                .body(Map.of("message", e.getMessage())); +                    }                  })                  .orElse(ResponseEntity.notFound().build());      } -} \ No newline at end of file +} diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/controller/ProjectController.java b/Chrono-backend/src/main/java/com/chrono/chrono/controller/ProjectController.java index 07e75259..36771bd2 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/controller/ProjectController.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/controller/ProjectController.java @@ -138,7 +138,7 @@ public class ProjectController {              if (parent.getId().equals(existing.getId())) {                  return ResponseEntity.badRequest().build();              } -            java.util.Set<Long> descendantIds = projectService.collectProjectAndDescendantIds(existing); +            Set<Long> descendantIds = projectService.collectProjectAndDescendantIds(existing);              if (descendantIds.contains(parent.getId())) {                  return ResponseEntity.status(409).build();              } @@ -158,7 +158,7 @@ public class ProjectController {      }        @DeleteMapping("/{id}") -    public ResponseEntity<Void> delete(@PathVariable Long id, Principal principal) { +    public ResponseEntity<?> delete(@PathVariable Long id, Principal principal) {          if (!featureEnabled(principal)) return ResponseEntity.status(403).build();          User user = userService.getUserByUsername(principal.getName());          return projectService.findById(id) @@ -166,10 +166,14 @@ public class ProjectController {                      if (existing.getCustomer() == null || !existing.getCustomer().getCompany().getId().equals(user.getCompany().getId())) {                          return ResponseEntity.status(403).<Void>build();                      } -                    projectService.deleteById(id); -                    complianceAuditService.recordAction(user, "DELETE", "PROJECT", id, -                            "Projekt gelöscht: " + existing.getName()); -                    return ResponseEntity.noContent().<Void>build(); +                    try { +                        projectService.deleteById(id); +                        complianceAuditService.recordAction(user, "DELETE", "PROJECT", id, +                                "Projekt geloescht: " + existing.getName()); +                        return ResponseEntity.noContent().<Void>build(); +                    } catch (IllegalStateException e) { +                        return ResponseEntity.status(409).body(Map.of("message", e.getMessage())); +                    }                  })                  .orElse(ResponseEntity.notFound().build());      } diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/controller/TaskController.java b/Chrono-backend/src/main/java/com/chrono/chrono/controller/TaskController.java index a8904412..c94a8f14 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/controller/TaskController.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/controller/TaskController.java @@ -12,6 +12,7 @@ import org.springframework.web.bind.annotation.*;    import java.security.Principal;  import java.util.List; +import java.util.Map;  import java.util.Optional;    @RestController @@ -84,7 +85,7 @@ public class TaskController {      }        @DeleteMapping("/{id}") -    public ResponseEntity<Void> deleteTask(@PathVariable Long id, Principal principal) { +    public ResponseEntity<?> deleteTask(@PathVariable Long id, Principal principal) {          if (!featureEnabled(principal)) return ResponseEntity.status(403).build();          User user = userService.getUserByUsername(principal.getName());          Optional<Task> existingOpt = taskService.findById(id); @@ -96,7 +97,11 @@ public class TaskController {              !existing.getProject().getCustomer().getCompany().getId().equals(user.getCompany().getId())) {              return ResponseEntity.status(403).build();          } -        taskService.deleteById(id); -        return ResponseEntity.noContent().build(); +        try { +            taskService.deleteById(id); +            return ResponseEntity.noContent().build(); +        } catch (IllegalStateException e) { +            return ResponseEntity.status(409).body(Map.of("message", e.getMessage())); +        }      }  } diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/controller/TimeTrackingController.java b/Chrono-backend/src/main/java/com/chrono/chrono/controller/TimeTrackingController.java index 6ffd6882..b80e63c1 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/controller/TimeTrackingController.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/controller/TimeTrackingController.java @@ -35,7 +35,7 @@ public class TimeTrackingController {          @PostMapping("/punch") -    public ResponseEntity<TimeTrackingEntryDTO> punch( +    public ResponseEntity<?> punch(          @RequestParam String username,          @RequestParam(value = "customerId", required = false) Long customerId,          @RequestParam(value = "projectId", required = false) Long projectId, @@ -90,8 +90,13 @@ public class TimeTrackingController {          try {              TimeTrackingEntryDTO newEntry = timeTrackingService.handlePunch(username, punchSource, customerId, projectId, taskId, durationMinutes, description);              return ResponseEntity.ok(newEntry); +        } catch (IllegalArgumentException | IllegalStateException e) { +            return ResponseEntity.badRequest().body(Map.of("message", e.getMessage())); +        } catch (SecurityException e) { +            return ResponseEntity.status(HttpStatus.FORBIDDEN).body(Map.of("message", e.getMessage()));          } catch (Exception e) { -            return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(null); +            return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR) +                    .body(Map.of("message", "Fehler beim Stempeln."));          }      }   @@ -194,8 +199,10 @@ public class TimeTrackingController {              Principal principal) {          if (principal == null) return ResponseEntity.status(HttpStatus.UNAUTHORIZED).build();          try { -            timeTrackingService.assignCustomerForDay(username, LocalDate.parse(date), customerId); +            timeTrackingService.assignCustomerForDay(username, LocalDate.parse(date), customerId, principal.getName());              return ResponseEntity.ok().build(); +        } catch (SecurityException e) { +            return ResponseEntity.status(HttpStatus.FORBIDDEN).build();          } catch (RuntimeException e) {              return ResponseEntity.badRequest().build();          } @@ -232,8 +239,11 @@ public class TimeTrackingController {                      LocalDate.parse(date),                      LocalTime.parse(startTime),                      LocalTime.parse(endTime), -                    customerId); +                    customerId, +                    principal.getName());              return ResponseEntity.ok().build(); +        } catch (SecurityException e) { +            return ResponseEntity.status(HttpStatus.FORBIDDEN).build();          } catch (RuntimeException e) {              return ResponseEntity.badRequest().build();          } @@ -263,8 +273,10 @@ public class TimeTrackingController {              Principal principal) {          if (principal == null) return ResponseEntity.status(HttpStatus.UNAUTHORIZED).build();          try { -            timeTrackingService.assignProjectForDay(username, LocalDate.parse(date), projectId); +            timeTrackingService.assignProjectForDay(username, LocalDate.parse(date), projectId, principal.getName());              return ResponseEntity.ok().build(); +        } catch (SecurityException e) { +            return ResponseEntity.status(HttpStatus.FORBIDDEN).build();          } catch (RuntimeException e) {              return ResponseEntity.badRequest().build();          } @@ -280,8 +292,10 @@ public class TimeTrackingController {          String note = body.get("note");          if (note == null) note = "";          try { -            timeTrackingService.saveDailyNote(username, LocalDate.parse(date), note); +            timeTrackingService.saveDailyNote(username, LocalDate.parse(date), note, principal.getName());              return ResponseEntity.ok().build(); +        } catch (SecurityException e) { +            return ResponseEntity.status(HttpStatus.FORBIDDEN).build();          } catch (RuntimeException e) {              return ResponseEntity.badRequest().build();          } diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/repositories/ProjectRepository.java b/Chrono-backend/src/main/java/com/chrono/chrono/repositories/ProjectRepository.java index a76c77d0..66e24872 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/repositories/ProjectRepository.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/repositories/ProjectRepository.java @@ -13,4 +13,8 @@ public interface ProjectRepository extends JpaRepository<Project, Long> {      java.util.List<Project> findByParentId(Long parentId);        void deleteByCustomerCompanyId(Long companyId); + +    boolean existsByCustomer_Id(Long customerId); + +    boolean existsByParent_Id(Long parentId);  } diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/repositories/TaskRepository.java b/Chrono-backend/src/main/java/com/chrono/chrono/repositories/TaskRepository.java index 2b1a8e3d..5b54cea4 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/repositories/TaskRepository.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/repositories/TaskRepository.java @@ -9,4 +9,6 @@ import java.util.List;  @Repository  public interface TaskRepository extends JpaRepository<Task, Long> {      List<Task> findByProjectId(Long projectId); + +    boolean existsByProjectId(Long projectId);  } diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/repositories/TimeTrackingEntryRepository.java b/Chrono-backend/src/main/java/com/chrono/chrono/repositories/TimeTrackingEntryRepository.java index 48c05cdb..f599f56e 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/repositories/TimeTrackingEntryRepository.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/repositories/TimeTrackingEntryRepository.java @@ -63,4 +63,10 @@ public interface TimeTrackingEntryRepository extends JpaRepository<TimeTrackingE      List<TimeTrackingEntry> findByProjectIdInAndEntryTimestampBetween(List<Long> projectIds,                                                                       LocalDateTime startDateTime,                                                                       LocalDateTime endDateTime); + +    boolean existsByCustomer_Id(Long customerId); + +    boolean existsByProject_Id(Long projectId); + +    boolean existsByTask_Id(Long taskId);  } diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/repositories/UserRepository.java b/Chrono-backend/src/main/java/com/chrono/chrono/repositories/UserRepository.java index a3cec509..60a971b7 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/repositories/UserRepository.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/repositories/UserRepository.java @@ -4,7 +4,9 @@ import com.chrono.chrono.entities.User;  import jakarta.persistence.LockModeType;  import org.springframework.data.jpa.repository.JpaRepository;  import org.springframework.data.jpa.repository.Lock; +import org.springframework.data.jpa.repository.Modifying;  import org.springframework.data.jpa.repository.Query; +import org.springframework.data.repository.query.Param;    import java.util.List;  import java.util.Optional; @@ -15,6 +17,12 @@ public interface UserRepository extends JpaRepository<User, Long> {      List<User> findByCompany_Id(Long companyId);      List<User> findByCompany_IdAndDeletedFalse(Long companyId);      List<User> findByDeletedFalse(); +    boolean existsByLastCustomer_Id(Long customerId); + +    @Modifying +    @Query("UPDATE User u SET u.lastCustomer = null WHERE u.lastCustomer.id = :customerId") +    void clearLastCustomerByCustomerId(@Param("customerId") Long customerId); +      @Lock(LockModeType.PESSIMISTIC_WRITE)      @Query("SELECT u FROM User u WHERE u.id = :id")      Optional<User> findByIdForUpdate(Long id); diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/services/CorrectionRequestService.java b/Chrono-backend/src/main/java/com/chrono/chrono/services/CorrectionRequestService.java index 0ed9987a..0367fcb1 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/services/CorrectionRequestService.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/services/CorrectionRequestService.java @@ -20,8 +20,10 @@ import org.springframework.transaction.annotation.Transactional;  import java.security.Principal;  import java.time.LocalDate;  import java.time.LocalDateTime; +import java.util.Comparator;  import java.util.Collections;  import java.util.List; +import java.util.Objects;  import java.util.Optional;  import java.util.stream.Collectors;   @@ -42,6 +44,78 @@ public class CorrectionRequestService {      @PersistenceContext      private EntityManager entityManager;   +    private void validateCorrectionRequest(User user, +                                           TimeTrackingEntry targetEntry, +                                           LocalDateTime desiredTimestamp, +                                           String reason, +                                           LocalDate requestDate) { +        if (requestDate == null) { +            throw new IllegalArgumentException("Das Antragsdatum ist erforderlich."); +        } +        if (desiredTimestamp == null) { +            throw new IllegalArgumentException("Der gewünschte Zeitstempel ist erforderlich."); +        } +        if (!desiredTimestamp.toLocalDate().equals(requestDate)) { +            throw new IllegalArgumentException("Der gewünschte Zeitstempel muss auf dem gewählten Korrekturtag liegen."); +        } +        if (reason == null || reason.trim().isEmpty()) { +            throw new IllegalArgumentException("Bitte einen Grund für die Korrektur angeben."); +        } +        if (targetEntry != null) { +            if (targetEntry.getUser() == null || !Objects.equals(targetEntry.getUser().getId(), user.getId())) { +                throw new IllegalArgumentException("Der gewählte Stempel gehört nicht zum Benutzer."); +            } +            if (targetEntry.getEntryDate() != null && !Objects.equals(targetEntry.getEntryDate(), requestDate)) { +                throw new IllegalArgumentException("Der gewählte Stempel liegt nicht auf dem beantragten Tag."); +            } +        } +    } + +    private void assertAdminCanManageUser(String adminUsername, User targetUser) { +        User admin = userRepo.findByUsername(adminUsername) +                .orElseThrow(() -> new UserNotFoundException("Admin user " + adminUsername + " not found")); + +        boolean isSuperAdmin = admin.getRoles().stream() +                .anyMatch(role -> role.getRoleName().equals("ROLE_SUPERADMIN")); +        boolean isAdminOfSameCompany = admin.getRoles().stream() +                .anyMatch(role -> role.getRoleName().equals("ROLE_ADMIN")) +                && admin.getCompany() != null +                && targetUser.getCompany() != null +                && Objects.equals(admin.getCompany().getId(), targetUser.getCompany().getId()); + +        if (!isSuperAdmin && !isAdminOfSameCompany) { +            throw new SecurityException("Keine Berechtigung für diesen Korrekturantrag."); +        } +    } + +    private void validateCorrectionSequence(List<CorrectionRequest> requests) { +        if (requests == null || requests.isEmpty()) { +            throw new IllegalStateException("Keine gültigen Korrekturanfragen für den Tag gefunden."); +        } +        if (requests.size() % 2 != 0) { +            throw new IllegalArgumentException("Korrekturen müssen aus einer geraden Anzahl von Stempeln bestehen."); +        } + +        TimeTrackingEntry.PunchType expectedType = TimeTrackingEntry.PunchType.START; +        LocalDateTime previousTimestamp = null; + +        for (CorrectionRequest request : requests) { +            if (request.getDesiredTimestamp() == null) { +                throw new IllegalArgumentException("Jeder Korrektureintrag braucht einen Zeitstempel."); +            } +            if (request.getDesiredPunchType() != expectedType) { +                throw new IllegalArgumentException("Korrekturen müssen mit START beginnen und START/ENDE sauber abwechseln."); +            } +            if (previousTimestamp != null && !request.getDesiredTimestamp().isAfter(previousTimestamp)) { +                throw new IllegalArgumentException("Korrekturzeiten müssen streng aufsteigend sein."); +            } +            previousTimestamp = request.getDesiredTimestamp(); +            expectedType = expectedType == TimeTrackingEntry.PunchType.START +                    ? TimeTrackingEntry.PunchType.ENDE +                    : TimeTrackingEntry.PunchType.START; +        } +    } +      @Transactional      public CorrectionRequest createCorrectionRequest(String username, Long targetEntryId,                                             LocalDateTime desiredTimestamp, String desiredPunchTypeStr, @@ -51,7 +125,8 @@ public class CorrectionRequestService {            TimeTrackingEntry targetEntry = null;          if (targetEntryId != null) { -            targetEntry = timeTrackingEntryRepo.findById(targetEntryId).orElse(null); +            targetEntry = timeTrackingEntryRepo.findById(targetEntryId) +                    .orElseThrow(() -> new IllegalArgumentException("Zielstempel nicht gefunden."));          }            TimeTrackingEntry.PunchType desiredPunchType; @@ -61,6 +136,8 @@ public class CorrectionRequestService {              throw new IllegalArgumentException("Ungültiger desiredPunchType: " + desiredPunchTypeStr);          }   +        validateCorrectionRequest(user, targetEntry, desiredTimestamp, reason, requestDate); +          CorrectionRequest req;          if (targetEntry != null) {              req = new CorrectionRequest(user, requestDate, targetEntry, desiredTimestamp, reason); @@ -128,6 +205,7 @@ public class CorrectionRequestService {            User targetUser = userRepo.findByIdForUpdate(userId)                  .orElseThrow(() -> new UserNotFoundException("Benutzer mit ID " + userId + " nicht gefunden.")); +        assertAdminCanManageUser(adminUsername, targetUser);            entityManager.clear(); // Dies schadet nicht und sorgt für zusätzliche Sicherheit auf JPA-Ebene.   @@ -147,9 +225,10 @@ public class CorrectionRequestService {          List<CorrectionRequest> allRequestsForDay = correctionRepo.findByUserAndDesiredTimestampBetweenAndApprovedIsFalseAndDeniedIsFalse(                  targetUser, startOfDay, endOfDay);   -        if (allRequestsForDay.isEmpty()) { -            throw new IllegalStateException("Keine gültigen Korrekturanfragen für den Tag gefunden."); -        } +        List<CorrectionRequest> sortedRequestsForDay = allRequestsForDay.stream() +                .sorted(Comparator.comparing(CorrectionRequest::getDesiredTimestamp)) +                .collect(Collectors.toList()); +        validateCorrectionSequence(sortedRequestsForDay);            List<TimeTrackingEntry> entriesToDelete = timeTrackingEntryRepo.findByUserAndEntryTimestampBetweenOrderByEntryTimestampAsc(targetUser, startOfDay, endOfDay);          if (!entriesToDelete.isEmpty()) { @@ -158,7 +237,7 @@ public class CorrectionRequestService {                      adminUsername, entriesToDelete.size(), targetUser.getUsername(), correctionDate);          }   -        for (CorrectionRequest req : allRequestsForDay) { +        for (CorrectionRequest req : sortedRequestsForDay) {              TimeTrackingEntry newEntry = new TimeTrackingEntry(                      req.getUser(),                      req.getDesiredTimestamp(), @@ -172,7 +251,7 @@ public class CorrectionRequestService {                      adminUsername, targetUser.getUsername(), newEntry.getEntryTimestamp(), newEntry.getPunchType());          }   -        for (CorrectionRequest req : allRequestsForDay) { +        for (CorrectionRequest req : sortedRequestsForDay) {              req.setApproved(true);              req.setDenied(false);              req.setAdminComment(comment); @@ -201,9 +280,10 @@ public class CorrectionRequestService {      }        @Transactional -    public CorrectionRequest denyRequest(Long requestId, String comment) { +    public CorrectionRequest denyRequest(Long requestId, String comment, String adminUsername) {          CorrectionRequest req = correctionRepo.findById(requestId)                  .orElseThrow(() -> new RuntimeException("Correction Request with ID " + requestId + " not found")); +        assertAdminCanManageUser(adminUsername, req.getUser());          if (req.isApproved() || req.isDenied()) {              throw new RuntimeException("Request with ID " + requestId + " has already been processed.");          } diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/services/CustomerService.java b/Chrono-backend/src/main/java/com/chrono/chrono/services/CustomerService.java index d906810f..67bf79ac 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/services/CustomerService.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/services/CustomerService.java @@ -2,8 +2,12 @@ package com.chrono.chrono.services;    import com.chrono.chrono.entities.Customer;  import com.chrono.chrono.repositories.CustomerRepository; +import com.chrono.chrono.repositories.ProjectRepository; +import com.chrono.chrono.repositories.TimeTrackingEntryRepository; +import com.chrono.chrono.repositories.UserRepository;  import org.springframework.beans.factory.annotation.Autowired;  import org.springframework.stereotype.Service; +import org.springframework.transaction.annotation.Transactional;    import java.util.List;  import java.util.Optional; @@ -12,6 +16,12 @@ import java.util.Optional;  public class CustomerService {      @Autowired      private CustomerRepository customerRepository; +    @Autowired +    private ProjectRepository projectRepository; +    @Autowired +    private TimeTrackingEntryRepository timeTrackingEntryRepository; +    @Autowired +    private UserRepository userRepository;      public List<Customer> findAllByCompanyId(Long companyId) {          return customerRepository.findByCompanyId(companyId);      } @@ -27,7 +37,19 @@ public class CustomerService {          return customerRepository.save(customer);      }   +    @Transactional      public void deleteById(Long id) { +        if (projectRepository.existsByCustomer_Id(id)) { +            throw new IllegalStateException("Kunde kann nicht gelöscht werden, weil noch Projekte daran hängen."); +        } +        if (timeTrackingEntryRepository.existsByCustomer_Id(id)) { +            throw new IllegalStateException("Kunde kann nicht gelöscht werden, weil noch Zeiteinträge daran hängen."); +        } + +        if (userRepository.existsByLastCustomer_Id(id)) { +            userRepository.clearLastCustomerByCustomerId(id); +        } +          customerRepository.deleteById(id);      }  } diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/services/ProjectService.java b/Chrono-backend/src/main/java/com/chrono/chrono/services/ProjectService.java index 9b50edce..0771933e 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/services/ProjectService.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/services/ProjectService.java @@ -2,6 +2,8 @@ package com.chrono.chrono.services;    import com.chrono.chrono.entities.Project;  import com.chrono.chrono.repositories.ProjectRepository; +import com.chrono.chrono.repositories.TaskRepository; +import com.chrono.chrono.repositories.TimeTrackingEntryRepository;  import org.springframework.beans.factory.annotation.Autowired;  import org.springframework.stereotype.Service;   @@ -16,6 +18,10 @@ import java.util.Set;  public class ProjectService {      @Autowired      private ProjectRepository projectRepository; +    @Autowired +    private TaskRepository taskRepository; +    @Autowired +    private TimeTrackingEntryRepository timeTrackingEntryRepository;        public List<Project> findAll() {          return projectRepository.findAll(); @@ -46,6 +52,15 @@ public class ProjectService {      }        public void deleteById(Long id) { +        if (projectRepository.existsByParent_Id(id)) { +            throw new IllegalStateException("Projekt kann nicht gelöscht werden, weil noch Unterprojekte daran hängen."); +        } +        if (taskRepository.existsByProjectId(id)) { +            throw new IllegalStateException("Projekt kann nicht gelöscht werden, weil noch Aufgaben daran hängen."); +        } +        if (timeTrackingEntryRepository.existsByProject_Id(id)) { +            throw new IllegalStateException("Projekt kann nicht gelöscht werden, weil noch Zeiteinträge daran hängen."); +        }          projectRepository.deleteById(id);      }   diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/services/TaskService.java b/Chrono-backend/src/main/java/com/chrono/chrono/services/TaskService.java index 9a83b3a3..8fb03cb8 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/services/TaskService.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/services/TaskService.java @@ -2,6 +2,7 @@ package com.chrono.chrono.services;    import com.chrono.chrono.entities.Task;  import com.chrono.chrono.repositories.TaskRepository; +import com.chrono.chrono.repositories.TimeTrackingEntryRepository;  import org.springframework.beans.factory.annotation.Autowired;  import org.springframework.stereotype.Service;   @@ -12,12 +13,14 @@ import java.util.Optional;  public class TaskService {      @Autowired      private TaskRepository taskRepository; +    @Autowired +    private TimeTrackingEntryRepository timeTrackingEntryRepository;        public List<Task> getTasks(Long projectId) {          if (projectId != null) {              return taskRepository.findByProjectId(projectId);          } -        return taskRepository.findAll(); +        return List.of();      }        public Task save(Task task) { @@ -29,6 +32,9 @@ public class TaskService {      }        public void deleteById(Long id) { +        if (timeTrackingEntryRepository.existsByTask_Id(id)) { +            throw new IllegalStateException("Aufgabe kann nicht gelöscht werden, weil noch Zeiteinträge daran hängen."); +        }          taskRepository.deleteById(id);      }  } diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/services/TimeTrackingService.java b/Chrono-backend/src/main/java/com/chrono/chrono/services/TimeTrackingService.java index 5f954030..67c52579 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/services/TimeTrackingService.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/services/TimeTrackingService.java @@ -75,18 +75,10 @@ public class TimeTrackingService {      @Transactional      public TimeTrackingEntryDTO handlePunch(String username, TimeTrackingEntry.PunchSource source, Long customerId, Long projectId, Long taskId, Integer durationMinutes, String description) {          User user = loadUserByUsername(username); -        Customer customer = null; -        if (customerId != null && user.getCompany() != null && Boolean.TRUE.equals(user.getCompany().getCustomerTrackingEnabled())) { -            customer = customerRepository.findById(customerId).orElse(null); -        } -        Project project = null; -        if (projectId != null && user.getCompany() != null && Boolean.TRUE.equals(user.getCompany().getCustomerTrackingEnabled())) { -            project = projectRepository.findById(projectId).orElse(null); -        } -        Task task = null; -        if (taskId != null) { -            task = taskRepository.findById(taskId).orElse(null); -        } +        TimeTrackingAssignment requestedAssignment = resolveAssignment(user, customerId, projectId, taskId); +        Customer customer = requestedAssignment.customer; +        Project project = requestedAssignment.project; +        Task task = requestedAssignment.task;          // Default to CET/Berlin timezone for all automatic timestamps          LocalDate today = getCurrentBerlinDate();          LocalDateTime now = LocalDateTime.now(ZoneId.of("Europe/Berlin")).truncatedTo(ChronoUnit.MINUTES); @@ -113,6 +105,14 @@ public class TimeTrackingService {          if (nextPunchType == TimeTrackingEntry.PunchType.ENDE && project == null && lastEntryOpt.isPresent()) {              project = lastEntryOpt.get().getProject();          } +        if (nextPunchType == TimeTrackingEntry.PunchType.ENDE && task == null && lastEntryOpt.isPresent()) { +            task = lastEntryOpt.get().getTask(); +        } + +        TimeTrackingAssignment finalAssignment = normalizeAssignment(user, customer, project, task); +        customer = finalAssignment.customer; +        project = finalAssignment.project; +        task = finalAssignment.task;            TimeTrackingEntry newEntry = new TimeTrackingEntry(user, customer, project, now, nextPunchType, source);          newEntry.setTask(task); @@ -1125,64 +1125,252 @@ public class TimeTrackingService {          return result;      }   -    @Transactional -    public TimeTrackingEntryDTO updateEntryCustomer(Long id, Long customerId, String requestingUser) { -        User requester = loadUserByUsername(requestingUser); -        TimeTrackingEntry entry = timeTrackingEntryRepository.findById(id) -                .orElseThrow(() -> new RuntimeException("Entry not found: " + id)); -        User entryUser = entry.getUser(); -        boolean allowed = requester.getId().equals(entryUser.getId()) || -                requester.getRoles().stream().anyMatch(r -> r.getRoleName().equals("ROLE_SUPERADMIN")) || -                (requester.getRoles().stream().anyMatch(r -> r.getRoleName().equals("ROLE_ADMIN")) && -                        requester.getCompany() != null && entryUser.getCompany() != null && -                        requester.getCompany().getId().equals(entryUser.getCompany().getId())); -        if (!allowed) { +    private boolean isCustomerTrackingEnabled(User user) { +        return user != null +                && user.getCompany() != null +                && Boolean.TRUE.equals(user.getCompany().getCustomerTrackingEnabled()); +    } + +    private void assertCustomerTrackingEnabled(User user) { +        if (!isCustomerTrackingEnabled(user)) { +            throw new IllegalStateException("Kundenerfassung ist fuer diesen Benutzer nicht aktiviert."); +        } +    } + +    private boolean canManageUser(User requester, User target) { +        return requester != null +                && target != null +                && ( +                Objects.equals(requester.getId(), target.getId()) +                        || requester.getRoles().stream().anyMatch(r -> "ROLE_SUPERADMIN".equals(r.getRoleName())) +                        || (requester.getRoles().stream().anyMatch(r -> "ROLE_ADMIN".equals(r.getRoleName())) +                        && requester.getCompany() != null +                        && target.getCompany() != null +                        && Objects.equals(requester.getCompany().getId(), target.getCompany().getId())) +        ); +    } + +    private User assertUserAccess(String requestingUsername, String targetUsername) { +        User requester = loadUserByUsername(requestingUsername); +        User target = loadUserByUsername(targetUsername); +        if (!canManageUser(requester, target)) {              throw new SecurityException("Not allowed");          } -        if (entryUser.getCompany() == null || !Boolean.TRUE.equals(entryUser.getCompany().getCustomerTrackingEnabled())) { -            throw new IllegalStateException("Feature disabled"); +        return target; +    } + +    private boolean customerBelongsToUserCompany(User user, Customer customer) { +        return user != null +                && user.getCompany() != null +                && customer != null +                && customer.getCompany() != null +                && Objects.equals(user.getCompany().getId(), customer.getCompany().getId()); +    } + +    private boolean projectBelongsToUserCompany(User user, Project project) { +        return user != null +                && user.getCompany() != null +                && project != null +                && project.getCustomer() != null +                && project.getCustomer().getCompany() != null +                && Objects.equals(user.getCompany().getId(), project.getCustomer().getCompany().getId()); +    } + +    private boolean taskBelongsToUserCompany(User user, Task task) { +        return user != null +                && user.getCompany() != null +                && task != null +                && task.getProject() != null +                && task.getProject().getCustomer() != null +                && task.getProject().getCustomer().getCompany() != null +                && Objects.equals(user.getCompany().getId(), task.getProject().getCustomer().getCompany().getId()); +    } + +    private Customer resolveCustomerForUser(User user, Long customerId) { +        if (customerId == null) { +            return null; +        } +        assertCustomerTrackingEnabled(user); +        Customer customer = customerRepository.findById(customerId) +                .orElseThrow(() -> new IllegalArgumentException("Kunde nicht gefunden.")); +        if (!customerBelongsToUserCompany(user, customer)) { +            throw new IllegalArgumentException("Kunde gehoert nicht zur Firma des Benutzers."); +        } +        return customer; +    } + +    private Project resolveProjectForUser(User user, Long projectId) { +        if (projectId == null) { +            return null; +        } +        assertCustomerTrackingEnabled(user); +        Project project = projectRepository.findById(projectId) +                .orElseThrow(() -> new IllegalArgumentException("Projekt nicht gefunden.")); +        if (!projectBelongsToUserCompany(user, project)) { +            throw new IllegalArgumentException("Projekt gehoert nicht zur Firma des Benutzers."); +        } +        if (project.getCustomer() == null) { +            throw new IllegalArgumentException("Projekt ist keinem Kunden zugeordnet."); +        } +        return project; +    } + +    private Task resolveTaskForUser(User user, Long taskId) { +        if (taskId == null) { +            return null; +        } +        assertCustomerTrackingEnabled(user); +        Task task = taskRepository.findById(taskId) +                .orElseThrow(() -> new IllegalArgumentException("Aufgabe nicht gefunden.")); +        if (!taskBelongsToUserCompany(user, task)) { +            throw new IllegalArgumentException("Aufgabe gehoert nicht zur Firma des Benutzers."); +        } +        return task; +    } + +    private TimeTrackingAssignment resolveAssignment(User user, Long customerId, Long projectId, Long taskId) { +        if (customerId == null && projectId == null && taskId == null) { +            return new TimeTrackingAssignment(null, null, null); +        } + +        return normalizeAssignment( +                user, +                resolveCustomerForUser(user, customerId), +                resolveProjectForUser(user, projectId), +                resolveTaskForUser(user, taskId) +        ); +    } + +    private TimeTrackingAssignment normalizeAssignment(User user, Customer customer, Project project, Task task) { +        if (task != null) { +            if (!taskBelongsToUserCompany(user, task)) { +                throw new IllegalArgumentException("Aufgabe gehoert nicht zur Firma des Benutzers."); +            } +            if (task.getProject() == null) { +                throw new IllegalArgumentException("Aufgabe ist keinem Projekt zugeordnet."); +            } +            if (project == null) { +                project = task.getProject(); +            } else if (!Objects.equals(task.getProject().getId(), project.getId())) { +                throw new IllegalArgumentException("Aufgabe gehoert nicht zum gewaehlten Projekt."); +            }          } -        Customer customer = null; -        if (customerId != null) { -            customer = customerRepository.findById(customerId).orElse(null); + +        if (project != null) { +            if (!projectBelongsToUserCompany(user, project)) { +                throw new IllegalArgumentException("Projekt gehoert nicht zur Firma des Benutzers."); +            } +            if (project.getCustomer() == null) { +                throw new IllegalArgumentException("Projekt ist keinem Kunden zugeordnet."); +            } +            if (customer == null) { +                customer = project.getCustomer(); +            } else if (!Objects.equals(project.getCustomer().getId(), customer.getId())) { +                throw new IllegalArgumentException("Projekt gehoert nicht zum gewaehlten Kunden."); +            }          } + +        if (customer != null && !customerBelongsToUserCompany(user, customer)) { +            throw new IllegalArgumentException("Kunde gehoert nicht zur Firma des Benutzers."); +        } + +        return new TimeTrackingAssignment(customer, project, task); +    } + +    private void applyCustomerAssignment(TimeTrackingEntry entry, Customer customer) {          entry.setCustomer(customer); -        TimeTrackingEntry saved = timeTrackingEntryRepository.save(entry);   -        // update last customer if this was the most recent entry -        List<TimeTrackingEntry> latestEntries = timeTrackingEntryRepository -                .findByUserOrderByEntryTimestampDesc(entryUser); -        if (!latestEntries.isEmpty() && Objects.equals(latestEntries.get(0).getId(), saved.getId())) { -            entryUser.setLastCustomer(saved.getCustomer()); -            userRepository.save(entryUser); +        if (customer == null) { +            entry.setProject(null); +            entry.setTask(null); +            return; +        } + +        Project currentProject = entry.getProject(); +        if (currentProject != null) { +            if (currentProject.getCustomer() == null +                    || !Objects.equals(currentProject.getCustomer().getId(), customer.getId())) { +                entry.setProject(null); +                entry.setTask(null); +                return; +            } +        } + +        Task currentTask = entry.getTask(); +        if (currentTask != null) { +            if (currentTask.getProject() == null +                    || currentTask.getProject().getCustomer() == null +                    || !Objects.equals(currentTask.getProject().getCustomer().getId(), customer.getId())) { +                entry.setTask(null); +            } +        } +    } + +    private void applyProjectAssignment(TimeTrackingEntry entry, Project project) { +        if (project == null) { +            entry.setProject(null); +            entry.setTask(null); +            return; +        } + +        entry.setProject(project); +        entry.setCustomer(project.getCustomer()); + +        Task currentTask = entry.getTask(); +        if (currentTask != null) { +            if (currentTask.getProject() == null +                    || !Objects.equals(currentTask.getProject().getId(), project.getId())) { +                entry.setTask(null); +            } +        } +    } + +    private void syncLastCustomerFromLatestEntry(User user) { +        TimeTrackingEntry latest = timeTrackingEntryRepository.findByUserOrderByEntryTimestampDesc(user) +                .stream() +                .findFirst() +                .orElse(null); +        user.setLastCustomer(latest != null ? latest.getCustomer() : null); +        userRepository.save(user); +    } + +    private static final class TimeTrackingAssignment { +        private final Customer customer; +        private final Project project; +        private final Task task; + +        private TimeTrackingAssignment(Customer customer, Project project, Task task) { +            this.customer = customer; +            this.project = project; +            this.task = task;          } +    }   +    @Transactional +    public TimeTrackingEntryDTO updateEntryCustomer(Long id, Long customerId, String requestingUser) { +        TimeTrackingEntry entry = timeTrackingEntryRepository.findById(id) +                .orElseThrow(() -> new RuntimeException("Entry not found: " + id)); +        User entryUser = assertUserAccess(requestingUser, entry.getUser().getUsername()); +        assertCustomerTrackingEnabled(entryUser); + +        Customer customer = resolveCustomerForUser(entryUser, customerId); +        applyCustomerAssignment(entry, customer); +        TimeTrackingEntry saved = timeTrackingEntryRepository.save(entry); +        syncLastCustomerFromLatestEntry(entryUser);          return TimeTrackingEntryDTO.fromEntity(saved);      }        @Transactional      public TimeTrackingEntryDTO updateEntryProject(Long id, Long projectId, String requestingUser) { -        User requester = loadUserByUsername(requestingUser);          TimeTrackingEntry entry = timeTrackingEntryRepository.findById(id)                  .orElseThrow(() -> new RuntimeException("Entry not found: " + id)); -        User entryUser = entry.getUser(); -        boolean allowed = requester.getId().equals(entryUser.getId()) || -                requester.getRoles().stream().anyMatch(r -> r.getRoleName().equals("ROLE_SUPERADMIN")) || -                (requester.getRoles().stream().anyMatch(r -> r.getRoleName().equals("ROLE_ADMIN")) && -                        requester.getCompany() != null && entryUser.getCompany() != null && -                        requester.getCompany().getId().equals(entryUser.getCompany().getId())); -        if (!allowed) { -            throw new SecurityException("Not allowed"); -        } -        if (entryUser.getCompany() == null || !Boolean.TRUE.equals(entryUser.getCompany().getCustomerTrackingEnabled())) { -            throw new IllegalStateException("Feature disabled"); -        } -        Project project = null; -        if (projectId != null) { -            project = projectRepository.findById(projectId).orElse(null); -        } -        entry.setProject(project); +        User entryUser = assertUserAccess(requestingUser, entry.getUser().getUsername()); +        assertCustomerTrackingEnabled(entryUser); + +        Project project = resolveProjectForUser(entryUser, projectId); +        applyProjectAssignment(entry, project);          TimeTrackingEntry saved = timeTrackingEntryRepository.save(entry); +        syncLastCustomerFromLatestEntry(entryUser);          return TimeTrackingEntryDTO.fromEntity(saved);      }   @@ -1194,48 +1382,37 @@ public class TimeTrackingService {      }        @Transactional -    public void assignCustomerForDay(String username, LocalDate date, Long customerId) { -        User user = loadUserByUsername(username); -        if (user.getCompany() == null || !Boolean.TRUE.equals(user.getCompany().getCustomerTrackingEnabled())) { -            throw new IllegalStateException("Feature disabled"); -        } -        Customer customer = customerId != null ? customerRepository.findById(customerId).orElse(null) : null; +    public void assignCustomerForDay(String username, LocalDate date, Long customerId, String requestingUser) { +        User user = assertUserAccess(requestingUser, username); +        assertCustomerTrackingEnabled(user); + +        Customer customer = resolveCustomerForUser(user, customerId);          List<TimeTrackingEntry> entries = timeTrackingEntryRepository.findByUserAndEntryDateOrderByEntryTimestampAsc(user, date);          for (TimeTrackingEntry e : entries) { -            e.setCustomer(customer); +            applyCustomerAssignment(e, customer);          }          timeTrackingEntryRepository.saveAll(entries); - -        // update last customer if latest entry falls on this day -        TimeTrackingEntry latest = timeTrackingEntryRepository -                .findByUserOrderByEntryTimestampDesc(user) -                .stream().findFirst().orElse(null); -        if (latest != null && date.equals(latest.getEntryDate())) { -            user.setLastCustomer(customer); -            userRepository.save(user); -        } +        syncLastCustomerFromLatestEntry(user);      }        @Transactional -    public void assignProjectForDay(String username, LocalDate date, Long projectId) { -        User user = loadUserByUsername(username); -        if (user.getCompany() == null || !Boolean.TRUE.equals(user.getCompany().getCustomerTrackingEnabled())) { -            throw new IllegalStateException("Feature disabled"); -        } -        Project project = projectId != null ? projectRepository.findById(projectId).orElse(null) : null; +    public void assignProjectForDay(String username, LocalDate date, Long projectId, String requestingUser) { +        User user = assertUserAccess(requestingUser, username); +        assertCustomerTrackingEnabled(user); + +        Project project = resolveProjectForUser(user, projectId);          List<TimeTrackingEntry> entries = timeTrackingEntryRepository.findByUserAndEntryDateOrderByEntryTimestampAsc(user, date);          for (TimeTrackingEntry e : entries) { -            e.setProject(project); +            applyProjectAssignment(e, project);          }          timeTrackingEntryRepository.saveAll(entries); +        syncLastCustomerFromLatestEntry(user);      }        @Transactional -    public void assignCustomerForTimeRange(String username, LocalDate date, LocalTime startTime, LocalTime endTime, Long customerId) { -        User user = loadUserByUsername(username); -        if (user.getCompany() == null || !Boolean.TRUE.equals(user.getCompany().getCustomerTrackingEnabled())) { -            throw new IllegalStateException("Feature disabled"); -        } +    public void assignCustomerForTimeRange(String username, LocalDate date, LocalTime startTime, LocalTime endTime, Long customerId, String requestingUser) { +        User user = assertUserAccess(requestingUser, username); +        assertCustomerTrackingEnabled(user);          if (startTime == null || endTime == null) {              throw new IllegalArgumentException("startTime and endTime required");          } @@ -1244,28 +1421,21 @@ public class TimeTrackingService {              startTime = endTime;              endTime = tmp;          } -        Customer customer = customerId != null ? customerRepository.findById(customerId).orElse(null) : null; +        Customer customer = resolveCustomerForUser(user, customerId);          LocalDateTime startDt = LocalDateTime.of(date, startTime); -        LocalDateTime endDt = LocalDateTime.of(date, endTime); +        LocalDateTime endDt = LocalDateTime.of(date, endTime).plusNanos(1);          List<TimeTrackingEntry> entries = timeTrackingEntryRepository                  .findByUserAndEntryTimestampBetweenOrderByEntryTimestampAsc(user, startDt, endDt);          for (TimeTrackingEntry e : entries) { -            e.setCustomer(customer); +            applyCustomerAssignment(e, customer);          }          timeTrackingEntryRepository.saveAll(entries); - -        TimeTrackingEntry latest = timeTrackingEntryRepository -                .findByUserOrderByEntryTimestampDesc(user) -                .stream().findFirst().orElse(null); -        if (latest != null && !entries.isEmpty() && latest.getId().equals(entries.get(entries.size() - 1).getId())) { -            user.setLastCustomer(customer); -            userRepository.save(user); -        } +        syncLastCustomerFromLatestEntry(user);      }        @Transactional -    public void saveDailyNote(String username, LocalDate date, String note) { -        User user = loadUserByUsername(username); +    public void saveDailyNote(String username, LocalDate date, String note, String requestingUser) { +        User user = assertUserAccess(requestingUser, username);          DailyNote dailyNote = dailyNoteRepository.findByUserAndNoteDate(user, date).orElse(null);          if (dailyNote == null) {              dailyNote = new DailyNote(); diff --git a/Chrono-backend/src/main/java/com/chrono/chrono/services/VacationService.java b/Chrono-backend/src/main/java/com/chrono/chrono/services/VacationService.java index ac5d4960..ebd70689 100644 --- a/Chrono-backend/src/main/java/com/chrono/chrono/services/VacationService.java +++ b/Chrono-backend/src/main/java/com/chrono/chrono/services/VacationService.java @@ -58,6 +58,136 @@ public class VacationService {          return false;      }   +    private void validateDateRange(LocalDate start, LocalDate end, boolean halfDay) { +        if (start == null || end == null) { +            throw new IllegalArgumentException("Start- und Enddatum muessen gesetzt sein."); +        } +        if (end.isBefore(start)) { +            throw new IllegalArgumentException("Das Enddatum darf nicht vor dem Startdatum liegen."); +        } +        if (halfDay && !start.isEqual(end)) { +            throw new IllegalArgumentException("Halbtags Urlaub kann nur fuer einen einzelnen Tag beantragt werden."); +        } +    } + +    private Map<Integer, Double> calculateRequestedVacationDaysPerYear(User user, LocalDate start, LocalDate end, boolean halfDay) { +        Map<Integer, Double> requestedDaysPerYear = new HashMap<>(); +        if (user == null || start == null || end == null || end.isBefore(start)) { +            return requestedDaysPerYear; +        } + +        for (LocalDate date = start; !date.isAfter(end); date = date.plusDays(1)) { +            if (!isChargeableVacationDay(user, date)) { +                continue; +            } +            double increment = (halfDay && start.equals(end)) ? 0.5 : 1.0; +            requestedDaysPerYear.merge(date.getYear(), increment, Double::sum); +        } + +        return requestedDaysPerYear; +    } + +    private double calculateReservedVacationDaysExcludingRequest(User user, int year, Long excludedRequestId) { +        double reservedDays = 0.0; +        for (VacationRequest existing : vacationRepo.findByUser(user)) { +            if (existing.isUsesOvertime() || existing.isDenied()) { +                continue; +            } +            if (excludedRequestId != null && Objects.equals(existing.getId(), excludedRequestId)) { +                continue; +            } +            reservedDays += calculateRequestedVacationDaysPerYear( +                    user, +                    existing.getStartDate(), +                    existing.getEndDate(), +                    existing.isHalfDay() +            ).getOrDefault(year, 0.0); +        } +        return reservedDays; +    } + +    private int determineRequiredOvertimeDeductionMinutes(User user, +                                                          LocalDate start, +                                                          LocalDate end, +                                                          boolean halfDay, +                                                          Integer overtimeDeductionMinutes) { +        if (Boolean.TRUE.equals(user.getIsHourly())) { +            throw new IllegalArgumentException("Stundenbasierte Mitarbeitende koennen keinen Ueberstundenurlaub beantragen."); +        } + +        double requestedDays = calculateRequestedVacationDays(user, start, end, halfDay); +        if (requestedDays <= 0) { +            throw new IllegalArgumentException("Der gewaehlte Zeitraum enthaelt keine anrechenbaren Urlaubstage."); +        } + +        if (Boolean.TRUE.equals(user.getIsPercentage())) { +            if (overtimeDeductionMinutes == null || overtimeDeductionMinutes <= 0) { +                throw new IllegalArgumentException("Fuer prozentuale Mitarbeitende muessen abzuziehende Ueberstunden-Minuten angegeben werden."); +            } +            return overtimeDeductionMinutes; +        } + +        if (overtimeDeductionMinutes != null && overtimeDeductionMinutes > 0) { +            return overtimeDeductionMinutes; +        } + +        return Math.max((int) Math.round(requestedDays * getDailyVacationMinutes(user)), 0); +    } + +    private void validateVacationEntitlement(User user, LocalDate start, LocalDate end, boolean halfDay, Long excludedRequestId) { +        Map<Integer, Double> requestedDaysPerYear = calculateRequestedVacationDaysPerYear(user, start, end, halfDay); +        if (requestedDaysPerYear.isEmpty()) { +            throw new IllegalArgumentException("Der gewaehlte Zeitraum enthaelt keine anrechenbaren Urlaubstage."); +        } + +        for (Map.Entry<Integer, Double> entry : requestedDaysPerYear.entrySet()) { +            int year = entry.getKey(); +            double requestedDays = entry.getValue(); +            double entitlement = calculateAnnualVacationEntitlement(user, year); +            double reservedDays = calculateReservedVacationDaysExcludingRequest(user, year, excludedRequestId); +            double remainingDays = entitlement - reservedDays; + +            if (requestedDays - remainingDays > 0.0001d) { +                throw new IllegalArgumentException("Nicht genug Urlaubstage verfuegbar fuer " + year + "."); +            } +        } +    } + +    private Integer validateAndResolveOvertimeDeduction(User user, +                                                        LocalDate start, +                                                        LocalDate end, +                                                        boolean halfDay, +                                                        Integer overtimeDeductionMinutes) { +        int requiredMinutes = determineRequiredOvertimeDeductionMinutes(user, start, end, halfDay, overtimeDeductionMinutes); +        int currentBalance = user.getTrackingBalanceInMinutes() != null ? user.getTrackingBalanceInMinutes() : 0; +        if (requiredMinutes > currentBalance) { +            throw new IllegalArgumentException("Nicht genug Ueberstunden verfuegbar."); +        } +        return requiredMinutes; +    } + +    private Integer validateVacationRequest(User user, +                                            LocalDate start, +                                            LocalDate end, +                                            boolean halfDay, +                                            boolean usesOvertime, +                                            Integer overtimeDeductionMinutes, +                                            Long excludedRequestId) { +        validateDateRange(start, end, halfDay); + +        double requestedDays = calculateRequestedVacationDays(user, start, end, halfDay); +        if (requestedDays <= 0) { +            throw new IllegalArgumentException("Der gewaehlte Zeitraum enthaelt keine anrechenbaren Urlaubstage."); +        } + +        if (usesOvertime) { +            return validateAndResolveOvertimeDeduction(user, start, end, halfDay, overtimeDeductionMinutes); +        } + +        validateVacationEntitlement(user, start, end, halfDay, excludedRequestId); +        return null; +    } +        public VacationRequest createVacationRequest(String username, LocalDate start, LocalDate end,                                                   boolean halfDay, boolean usesOvertime, @@ -65,19 +195,17 @@ public class VacationService {          User user = userRepo.findByUsername(username)                  .orElseThrow(() -> new UserNotFoundException("User not found: " + username)); //   -        if (end.isBefore(start)) { // -            throw new IllegalArgumentException("Das Enddatum darf nicht vor dem Startdatum liegen."); // -        } -        if (halfDay && !start.isEqual(end)) { // -            throw new IllegalArgumentException("Halbtags Urlaub kann nur für einen einzelnen Tag beantragt werden."); // -        } - -        // REMOVED THE LINE BELOW, AS 'targetUser' IS NOT DEFINED HERE. -        // if (isConflicting(targetUser, start, end)) { -        //     throw new IllegalArgumentException("Urlaub kollidiert mit bestehenden genehmigten Anträgen."); -        // } - -        if (isConflicting(user, start, end)) { // Corrected line +        Integer resolvedOvertimeDeductionMinutes = validateVacationRequest( +                user, +                start, +                end, +                halfDay, +                usesOvertime, +                overtimeDeductionMinutes, +                null +        ); + +        if (isConflicting(user, start, end)) {              throw new IllegalArgumentException("Urlaubsantrag steht im Konflikt mit bestehenden genehmigten Anträgen.");          }   @@ -93,18 +221,11 @@ public class VacationService {          // An admin must explicitly approve or deny the request later.          vr.setApproved(false);   -        if (usesOvertime && Boolean.TRUE.equals(user.getIsPercentage())) { // -            if (overtimeDeductionMinutes != null && overtimeDeductionMinutes > 0) { // -                vr.setOvertimeDeductionMinutes(overtimeDeductionMinutes); // -            } else { -                vr.setOvertimeDeductionMinutes(null); // -                logger.warn("User {} (prozentual) hat Überstundenurlaub ohne gültige Abzugsminuten beantragt. `overtimeDeductionMinutes` wird auf NULL gesetzt.", username); // -            } -        } else if (usesOvertime && !Boolean.TRUE.equals(user.getIsPercentage())) { // -            vr.setOvertimeDeductionMinutes(null); // -        } else { -            vr.setOvertimeDeductionMinutes(null); // -        } +        vr.setOvertimeDeductionMinutes( +                usesOvertime && Boolean.TRUE.equals(user.getIsPercentage()) +                        ? resolvedOvertimeDeductionMinutes +                        : null +        );          VacationRequest saved = vacationRepo.save(vr); //          logger.info("VacationService: VacationRequest (ID: {}) für User '{}' von {} bis {} erstellt (usesOvertime={}, halfDay={}, overtimeDeductionMinutes={}).",                  saved.getId(), username, start, end, usesOvertime, halfDay, vr.getOvertimeDeductionMinutes()); // @@ -135,12 +256,15 @@ public class VacationService {              }          }   -        if (end.isBefore(start)) { // -            throw new IllegalArgumentException("Das Enddatum darf nicht vor dem Startdatum liegen."); // -        } -        if (halfDay && !start.isEqual(end)) { // -            throw new IllegalArgumentException("Halbtags Urlaub kann nur für einen einzelnen Tag beantragt werden."); // -        } +        Integer resolvedOvertimeDeductionMinutes = validateVacationRequest( +                targetUser, +                start, +                end, +                halfDay, +                usesOvertime, +                overtimeDeductionMinutesParam, +                null +        );            VacationRequest vr = new VacationRequest(); //          vr.setUser(targetUser); // @@ -150,12 +274,11 @@ public class VacationService {          vr.setDenied(false); //          vr.setHalfDay(halfDay); //          vr.setUsesOvertime(usesOvertime); // -        vr.setOvertimeDeductionMinutes(null); // - -        if (usesOvertime && Boolean.TRUE.equals(targetUser.getIsPercentage()) && overtimeDeductionMinutesParam != null && overtimeDeductionMinutesParam > 0) { // -            vr.setOvertimeDeductionMinutes(overtimeDeductionMinutesParam); // -            logger.info("Admin create for percentage user {}: overtimeDeductionMinutes set to {} for the period.", targetUser.getUsername(), overtimeDeductionMinutesParam); // -        } +        vr.setOvertimeDeductionMinutes( +                usesOvertime && Boolean.TRUE.equals(targetUser.getIsPercentage()) +                        ? resolvedOvertimeDeductionMinutes +                        : null +        ); //              VacationRequest savedVr = vacationRepo.save(vr); // @@ -227,6 +350,23 @@ public class VacationService {              return vr; //          }   +        if (!vr.isCompanyVacation()) { +            Integer resolvedOvertimeDeductionMinutes = validateVacationRequest( +                    user, +                    vr.getStartDate(), +                    vr.getEndDate(), +                    vr.isHalfDay(), +                    vr.isUsesOvertime(), +                    vr.getOvertimeDeductionMinutes(), +                    vr.getId() +            ); +            vr.setOvertimeDeductionMinutes( +                    vr.isUsesOvertime() && Boolean.TRUE.equals(user.getIsPercentage()) +                            ? resolvedOvertimeDeductionMinutes +                            : null +            ); +        } +          vr.setApproved(true); //          vr.setDenied(false); //   @@ -498,17 +638,8 @@ public class VacationService {          LocalDate effectiveStart = newStartDate != null ? newStartDate : vr.getStartDate();          LocalDate effectiveEnd = newEndDate != null ? newEndDate : vr.getEndDate();   -        if (effectiveStart == null || effectiveEnd == null) { -            throw new IllegalArgumentException("Start- und Enddatum dürfen nicht leer sein."); -        } -        if (effectiveEnd.isBefore(effectiveStart)) { -            throw new IllegalArgumentException("Das Enddatum darf nicht vor dem Startdatum liegen."); -        } -          boolean newHalfDay = halfDayParam != null ? halfDayParam : vr.isHalfDay(); -        if (newHalfDay && !effectiveStart.isEqual(effectiveEnd)) { -            throw new IllegalArgumentException("Halbtags Urlaub kann nur für einen einzelnen Tag eingetragen werden."); -        } +        validateDateRange(effectiveStart, effectiveEnd, newHalfDay);            boolean previousApproved = vr.isApproved();          boolean previousUsesOvertime = vr.isUsesOvertime(); @@ -545,13 +676,21 @@ public class VacationService {                  ? overtimeDeductionMinutesParam                  : vr.getOvertimeDeductionMinutes();   -        if (!newUsesOvertime) { -            vr.setOvertimeDeductionMinutes(null); -        } else if (Boolean.TRUE.equals(targetUser.getIsPercentage())) { -            if (effectiveOvertimeDeductionMinutes == null || effectiveOvertimeDeductionMinutes <= 0) { -                throw new IllegalArgumentException("Für prozentuale Mitarbeitende müssen abzuziehende Überstunden-Minuten angegeben werden."); -            } -            vr.setOvertimeDeductionMinutes(effectiveOvertimeDeductionMinutes); +        if (!vr.isCompanyVacation()) { +            Integer validatedOvertimeDeductionMinutes = validateVacationRequest( +                    targetUser, +                    effectiveStart, +                    effectiveEnd, +                    newHalfDay, +                    newUsesOvertime, +                    effectiveOvertimeDeductionMinutes, +                    vr.getId() +            ); +            vr.setOvertimeDeductionMinutes( +                    newUsesOvertime && Boolean.TRUE.equals(targetUser.getIsPercentage()) +                            ? validatedOvertimeDeductionMinutes +                            : null +            );          } else {              vr.setOvertimeDeductionMinutes(null);          } @@ -680,12 +819,9 @@ public class VacationService {          double days = 0.0;          for (LocalDate d = start; !d.isAfter(end); d = d.plusDays(1)) {              if (isChargeableVacationDay(user, d)) { -                days += 1.0; +                days += (halfDay && start.equals(end)) ? 0.5 : 1.0;              }          } -        if (halfDay && start.equals(end)) { -            days -= 0.5; -        }          return days;      }   diff --git a/Chrono-frontend/src/App.jsx b/Chrono-frontend/src/App.jsx index c4dee3f3..d0c3e959 100644 --- a/Chrono-frontend/src/App.jsx +++ b/Chrono-frontend/src/App.jsx @@ -69,6 +69,7 @@ function App() {                      <Routes>                          {/* Öffentliche Routen */}                          <Route path="/" element={<LandingPage />} /> +                        <Route path="/preise" element={<Navigate to="/register" replace />} />                          <Route path="/login" element={<Login />} />                          <Route path="/register" element={<Registration />} />                          <Route path="/agb" element={<AGB />} /> @@ -77,9 +78,17 @@ function App() {                            {/* Geschützte Benutzer-Routen */}                          <Route path="/dashboard" element={<PrivateRoute><UserDashboard /></PrivateRoute>} /> +                        <Route path="/user" element={<PrivateRoute><Navigate to="/dashboard" replace /></PrivateRoute>} />                          <Route path="/percentage-punch" element={<PrivateRoute><PercentagePunch /></PrivateRoute>} />                          <Route path="/personal-data" element={<PrivateRoute><PersonalDataPage /></PrivateRoute>} /> -                        <Route path="/payslips" element={<PrivateRoute><PayslipsPage /></PrivateRoute>} /> +                        <Route +                            path="/payslips" +                            element={ +                                <PrivateRoute requiredFeature="payroll" redirectTo="/dashboard"> +                                    <PayslipsPage /> +                                </PrivateRoute> +                            } +                        />                          <Route path="/demo-tour" element={<PrivateRoute><DemoTour /></PrivateRoute>} />                            {/* Admin-Routen */} diff --git a/Chrono-frontend/src/components/CorrectionModal.jsx b/Chrono-frontend/src/components/CorrectionModal.jsx index 3ded6613..f9c9b103 100644 --- a/Chrono-frontend/src/components/CorrectionModal.jsx +++ b/Chrono-frontend/src/components/CorrectionModal.jsx @@ -6,16 +6,17 @@ import { formatDate } from '../utils/dateUtils';  import { formatTime, sortEntries } from '../utils/timeUtils';    const CorrectionModal = ({ -                             visible, -                             correctionDate, -                             dailySummary, -                             onClose, -                             onSubmit, -                             t, -                             dashboard = 'user', // 'user' | 'hourly' | 'percentage' -                         }) => { +    visible, +    correctionDate, +    dailySummary, +    onClose, +    onSubmit, +    t, +    dashboard = 'user', +}) => {      const [entries, setEntries] = useState([]);      const [reason, setReason] = useState(''); +    const [validationError, setValidationError] = useState('');        useEffect(() => {          if (visible) { @@ -35,6 +36,7 @@ const CorrectionModal = ({                  setEntries(initialEntries);              }              setReason(''); +            setValidationError('');          }      }, [visible, dailySummary]);   @@ -46,32 +48,82 @@ const CorrectionModal = ({              next[index][field] = value;              return next;          }); +        setValidationError('');      };        const addEntry = () => {          const last = entries[entries.length - 1];          const nextType = last?.type === 'START' ? 'ENDE' : 'START';          setEntries([...entries, { time: '', type: nextType, targetEntryId: null }]); +        setValidationError('');      };        const removeEntry = (index) => {          setEntries((prev) => prev.filter((_, i) => i !== index)); +        setValidationError(''); +    }; + +    const validateEntries = () => { +        if (entries.length < 2) { +            return t('correctionModal.minimumEntries', 'Bitte gib mindestens einen Start- und einen Ende-Eintrag an.'); +        } + +        if (entries.length % 2 !== 0) { +            return t('correctionModal.evenEntries', 'Korrekturen müssen aus vollständigen Start/Ende-Paaren bestehen.'); +        } + +        let previousMinutes = null; +        for (let index = 0; index < entries.length; index += 1) { +            const entry = entries[index]; +            if (!entry.time) { +                return t('correctionModal.timeRequired', 'Bitte fÃ¼llen Sie alle Zeitfelder aus.'); +            } + +            const [hours, minutes] = entry.time.split(':').map(Number); +            const currentMinutes = (hours * 60) + minutes; +            if (!Number.isFinite(currentMinutes)) { +                return t('correctionModal.invalidTime', 'Bitte gib fÃ¼r jeden Eintrag eine gÃ¼ltige Uhrzeit an.'); +            } + +            const expectedType = index % 2 === 0 ? 'START' : 'ENDE'; +            if (entry.type !== expectedType) { +                return t( +                    'correctionModal.invalidSequence', +                    'Die EintrÃ¤ge mÃ¼ssen mit START beginnen und sich danach sauber mit ENDE abwechseln.' +                ); +            } + +            if (previousMinutes !== null && currentMinutes <= previousMinutes) { +                return t( +                    'correctionModal.ascendingTimes', +                    'Die Zeiten mÃ¼ssen in aufsteigender Reihenfolge eingegeben werden.' +                ); +            } + +            previousMinutes = currentMinutes; +        } + +        return '';      };        const handleSubmit = (e) => {          e.preventDefault(); +          if (!reason.trim()) { -            alert(t('correctionModal.reasonRequired', 'Bitte geben Sie einen Grund an.')); +            setValidationError(t('correctionModal.reasonRequired', 'Bitte geben Sie einen Grund an.'));              return;          } -        if (entries.some((e) => !e.time)) { -            alert(t('correctionModal.timeRequired', 'Bitte füllen Sie alle Zeitfelder aus.')); + +        const sequenceError = validateEntries(); +        if (sequenceError) { +            setValidationError(sequenceError);              return;          } + +        setValidationError('');          onSubmit(entries, reason);      };   -    // map dashboard -> data-context expected by the isolated CSS      const contextMap = {          user: 'correction-user',          hourly: 'correction-hourly', @@ -89,7 +141,7 @@ const CorrectionModal = ({              <div className="modal-content" data-context={dataContext}>                  <div className="modal-header">                      <h3> -                        {t('userCorrectionModal.title', 'Korrekturantrag für')}{' '} +                        {t('userCorrectionModal.title', 'Korrekturantrag fÃ¼r')}{' '}                          {formatDate(new Date(`${correctionDate}T00:00:00`))}                      </h3>                  </div> @@ -128,7 +180,7 @@ const CorrectionModal = ({                          </div>                            <button type="button" className="add-correction-entry" onClick={addEntry}> -                            + {t('correctionModal.addEntry', 'Eintrag hinzufügen')} +                            + {t('correctionModal.addEntry', 'Eintrag hinzufÃ¼gen')}                          </button>                            <div className="form-group" style={{ marginTop: '1rem' }}> @@ -136,17 +188,25 @@ const CorrectionModal = ({                              <textarea                                  id="reason-textarea"                                  value={reason} -                                onChange={(e) => setReason(e.target.value)} +                                onChange={(e) => { +                                    setReason(e.target.value); +                                    setValidationError(''); +                                }}                                  placeholder={t(                                      'userCorrectionModal.reasonPlaceholder', -                                    'Begründung für die Korrektur...' +                                    'BegrÃ¼ndung fÃ¼r die Korrektur...'                                  )}                                  required                                  rows={4}                              />                          </div>   -                        {/* Footer (sticky via CSS) */} +                        {validationError && ( +                            <div className="error-message" role="alert"> +                                {validationError} +                            </div> +                        )} +                          <div className="modal-footer">                              <button type="submit" className="button-primary">                                  {t('submitCorrection', 'Antrag senden')} diff --git a/Chrono-frontend/src/components/CustomerTimeAssignModal.jsx b/Chrono-frontend/src/components/CustomerTimeAssignModal.jsx index 83e187c7..2aff3d82 100644 --- a/Chrono-frontend/src/components/CustomerTimeAssignModal.jsx +++ b/Chrono-frontend/src/components/CustomerTimeAssignModal.jsx @@ -7,133 +7,181 @@ import { useNotification } from '../context/NotificationContext';  import { formatTime, minutesToHHMM } from '../utils/timeUtils';  import { formatDate, formatLocalDate } from '../utils/dateUtils';   +const getProjectCustomerId = (project) => { +    if (!project) { +        return ''; +    } +    return String(project.customer?.id ?? project.customerId ?? ''); +};    const createTimeBlocks = (entries) => { -  const blocks = []; -  let currentBlock = null; -  (entries || []).forEach(entry => { -    if (entry.punchType === 'START') { -      currentBlock = { -        startEntry: entry, -        endEntry: null, -        customerId: entry.customerId || '', -        projectId: entry.projectId || '', -      }; -    } else if (entry.punchType === 'ENDE' && currentBlock) { -      currentBlock.endEntry = entry; -      blocks.push(currentBlock); -      currentBlock = null; -    } -  }); -  return blocks; +    const blocks = []; +    let currentBlock = null; + +    (entries || []).forEach((entry) => { +        if (entry.punchType === 'START') { +            currentBlock = { +                startEntry: entry, +                endEntry: null, +                customerId: String(entry.customerId || entry.customer?.id || ''), +                projectId: String(entry.projectId || entry.project?.id || ''), +            }; +        } else if (entry.punchType === 'ENDE' && currentBlock) { +            currentBlock.endEntry = entry; +            blocks.push(currentBlock); +            currentBlock = null; +        } +    }); + +    return blocks;  };    const CustomerTimeAssignModal = ({ t, day, summary, customers, projects, onClose, onSave }) => { -  const [timeBlocks, setTimeBlocks] = useState([]); -  const { currentUser } = useAuth(); -  const { notify } = useNotification(); - -  useEffect(() => { -    const sorted = [...(summary?.entries || [])].sort((a, b) => new Date(a.entryTimestamp) - new Date(b.entryTimestamp)); -    setTimeBlocks(createTimeBlocks(sorted)); -  }, [day, summary]); - -  const handleBlockChange = (index, field, value) => { -    setTimeBlocks(prev => { -      const blocks = [...prev]; -      blocks[index] = { ...blocks[index], [field]: value }; -      return blocks; -    }); -  }; - -  const handleSave = async () => { -    const tasks = []; -    timeBlocks.forEach(block => { -      if (!block.startEntry || !block.endEntry) return; -      const customerParams = { -        username: currentUser.username, -        date: formatLocalDate(day), - -        startTime: formatTime(block.startEntry.entryTimestamp), -        endTime: formatTime(block.endEntry.entryTimestamp) -      }; -      if (block.customerId) customerParams.customerId = block.customerId; -      tasks.push(api.put('/api/timetracking/range/customer', null, { params: customerParams })); - -      const projectParams = projectId => ({ projectId: projectId || null }); -      if (block.startEntry.id) -        tasks.push(api.put(`/api/timetracking/entry/${block.startEntry.id}/project`, null, { params: projectParams(block.projectId) })); -      if (block.endEntry.id) -        tasks.push(api.put(`/api/timetracking/entry/${block.endEntry.id}/project`, null, { params: projectParams(block.projectId) })); +    const [timeBlocks, setTimeBlocks] = useState([]); +    const { currentUser } = useAuth(); +    const { notify } = useNotification();   -    }); -    try { -      await Promise.all(tasks); -      notify(t('assignCustomer.saveSuccess', 'Änderungen gespeichert!'), 'success'); -      onSave(); -    } catch (err) { -      console.error('Error saving time blocks:', err); -      notify(t('assignCustomer.saveError', 'Fehler beim Speichern.'), 'error'); -    } -  }; - -  const totalWorked = useMemo(() => minutesToHHMM(summary?.workedMinutes || 0), [summary]); - -  return ( -    <ModalOverlay visible onClose={onClose} className="modal-backdrop"> -      <div className="customer-assign-modal"> -        <header className="modal-header"> -          <h3>{t('assignCustomer.title', 'Zeiten & Kunden für')} {formatDate(day)}</h3> -          <p>{t('assignCustomer.totalWorked', 'Gesamtarbeitszeit')}: <strong>{totalWorked}</strong></p> -        </header> -        <div className="modal-body"> -          {timeBlocks.length === 0 ? ( -            <p className="no-blocks-message">{t('assignCustomer.noBlocks', 'Keine vollständigen Zeitblöcke zum Bearbeiten gefunden.')}</p> -          ) : ( -            timeBlocks.map((block, index) => ( -              <div key={index} className="time-block-card"> -                <div className="time-block-header"> -                  <span className="time-range"> -                    {formatTime(block.startEntry.entryTimestamp)} - {block.endEntry ? formatTime(block.endEntry.entryTimestamp) : '...'} -                  </span> -                </div> -                <div className="time-block-form"> -                  <div className="form-group"> -                    <label htmlFor={`customer-${index}`}>{t('customerLabel', 'Kunde')}</label> -                    <select id={`customer-${index}`} value={block.customerId} onChange={e => handleBlockChange(index, 'customerId', e.target.value)}> -                      <option value="">{t('noCustomer', 'Kein Kunde')}</option> -                      {customers.map(c => <option key={c.id} value={c.id}>{c.name}</option>)} -                    </select> -                  </div> -                  <div className="form-group"> -                    <label htmlFor={`project-${index}`}>{t('projectLabel', 'Projekt')}</label> -                    <select id={`project-${index}`} value={block.projectId} onChange={e => handleBlockChange(index, 'projectId', e.target.value)}> -                      <option value="">{t('noProject', 'Kein Projekt')}</option> -                      {projects.map(p => <option key={p.id} value={p.id}>{p.name}</option>)} -                    </select> -                  </div> +    useEffect(() => { +        const sorted = [...(summary?.entries || [])].sort( +            (a, b) => new Date(a.entryTimestamp) - new Date(b.entryTimestamp) +        ); +        setTimeBlocks(createTimeBlocks(sorted)); +    }, [day, summary]); + +    const handleBlockChange = (index, field, value) => { +        setTimeBlocks((prev) => { +            const blocks = [...prev]; +            const current = { ...blocks[index], [field]: value }; + +            if (field === 'customerId' && current.projectId) { +                const selectedProject = projects.find((project) => String(project.id) === String(current.projectId)); +                const projectCustomerId = getProjectCustomerId(selectedProject); +                if (projectCustomerId && projectCustomerId !== String(value || '')) { +                    current.projectId = ''; +                } +            } + +            blocks[index] = current; +            return blocks; +        }); +    }; + +    const handleSave = async () => { +        if (!currentUser?.username) { +            notify(t('assignCustomer.saveError', 'Fehler beim Speichern.'), 'error'); +            return; +        } + +        try { +            for (const block of timeBlocks) { +                if (!block.startEntry || !block.endEntry) { +                    continue; +                } + +                const customerParams = { +                    username: currentUser.username, +                    date: formatLocalDate(day), +                    startTime: formatTime(block.startEntry.entryTimestamp), +                    endTime: formatTime(block.endEntry.entryTimestamp), +                }; +                if (block.customerId) { +                    customerParams.customerId = block.customerId; +                } +                await api.put('/api/timetracking/range/customer', null, { params: customerParams }); + +                const projectParams = block.projectId ? { projectId: block.projectId } : { projectId: null }; +                if (block.startEntry.id) { +                    await api.put(`/api/timetracking/entry/${block.startEntry.id}/project`, null, { params: projectParams }); +                } +                if (block.endEntry.id) { +                    await api.put(`/api/timetracking/entry/${block.endEntry.id}/project`, null, { params: projectParams }); +                } +            } + +            notify(t('assignCustomer.saveSuccess', 'Ã„nderungen gespeichert!'), 'success'); +            onSave(); +        } catch (err) { +            console.error('Error saving time blocks:', err); +            const message = err.response?.data?.message || t('assignCustomer.saveError', 'Fehler beim Speichern.'); +            notify(message, 'error'); +        } +    }; + +    const totalWorked = useMemo(() => minutesToHHMM(summary?.workedMinutes || 0), [summary]); + +    return ( +        <ModalOverlay visible onClose={onClose} className="modal-backdrop"> +            <div className="customer-assign-modal"> +                <header className="modal-header"> +                    <h3>{t('assignCustomer.title', 'Zeiten & Kunden fÃ¼r')} {formatDate(day)}</h3> +                    <p>{t('assignCustomer.totalWorked', 'Gesamtarbeitszeit')}: <strong>{totalWorked}</strong></p> +                </header> +                <div className="modal-body"> +                    {timeBlocks.length === 0 ? ( +                        <p className="no-blocks-message">{t('assignCustomer.noBlocks', 'Keine vollstÃ¤ndigen ZeitblÃ¶cke zum Bearbeiten gefunden.')}</p> +                    ) : ( +                        timeBlocks.map((block, index) => { +                            const availableProjects = block.customerId +                                ? projects.filter((project) => getProjectCustomerId(project) === String(block.customerId)) +                                : projects; + +                            return ( +                                <div key={index} className="time-block-card"> +                                    <div className="time-block-header"> +                                        <span className="time-range"> +                                            {formatTime(block.startEntry.entryTimestamp)} - {block.endEntry ? formatTime(block.endEntry.entryTimestamp) : '...'} +                                        </span> +                                    </div> +                                    <div className="time-block-form"> +                                        <div className="form-group"> +                                            <label htmlFor={`customer-${index}`}>{t('customerLabel', 'Kunde')}</label> +                                            <select +                                                id={`customer-${index}`} +                                                value={block.customerId} +                                                onChange={(e) => handleBlockChange(index, 'customerId', e.target.value)} +                                            > +                                                <option value="">{t('noCustomer', 'Kein Kunde')}</option> +                                                {customers.map((customer) => ( +                                                    <option key={customer.id} value={customer.id}>{customer.name}</option> +                                                ))} +                                            </select> +                                        </div> +                                        <div className="form-group"> +                                            <label htmlFor={`project-${index}`}>{t('projectLabel', 'Projekt')}</label> +                                            <select +                                                id={`project-${index}`} +                                                value={block.projectId} +                                                onChange={(e) => handleBlockChange(index, 'projectId', e.target.value)} +                                            > +                                                <option value="">{t('noProject', 'Kein Projekt')}</option> +                                                {availableProjects.map((project) => ( +                                                    <option key={project.id} value={project.id}>{project.name}</option> +                                                ))} +                                            </select> +                                        </div> +                                    </div> +                                </div> +                            ); +                        }) +                    )}                  </div> -              </div> -            )) -          )} -        </div> -        <footer className="modal-footer"> -          <button onClick={onClose} className="button-secondary">{t('cancel', 'Abbrechen')}</button> -          <button onClick={handleSave} className="button-primary">{t('save', 'Speichern')}</button> -        </footer> -      </div> -    </ModalOverlay> -  ); +                <footer className="modal-footer"> +                    <button onClick={onClose} className="button-secondary">{t('cancel', 'Abbrechen')}</button> +                    <button onClick={handleSave} className="button-primary">{t('save', 'Speichern')}</button> +                </footer> +            </div> +        </ModalOverlay> +    );  };    CustomerTimeAssignModal.propTypes = { -  t: PropTypes.func.isRequired, -  day: PropTypes.instanceOf(Date).isRequired, -  summary: PropTypes.object, -  customers: PropTypes.array.isRequired, -  projects: PropTypes.array.isRequired, -  onClose: PropTypes.func.isRequired, -  onSave: PropTypes.func.isRequired, +    t: PropTypes.func.isRequired, +    day: PropTypes.instanceOf(Date).isRequired, +    summary: PropTypes.object, +    customers: PropTypes.array.isRequired, +    projects: PropTypes.array.isRequired, +    onClose: PropTypes.func.isRequired, +    onSave: PropTypes.func.isRequired,  };    export default CustomerTimeAssignModal; diff --git a/Chrono-frontend/src/components/MobileTabBar.jsx b/Chrono-frontend/src/components/MobileTabBar.jsx index f3b588fd..cdad46a4 100644 --- a/Chrono-frontend/src/components/MobileTabBar.jsx +++ b/Chrono-frontend/src/components/MobileTabBar.jsx @@ -1,34 +1,68 @@  import { NavLink, useLocation } from "react-router-dom"; +import { useMemo } from "react"; +import { useAuth } from "../context/AuthContext";  import "../styles/MobileTabBar.css";   -const USER_NAV = [ -    { to: "/dashboard", label: "Start", icon: "🏠" }, -    { to: "/percentage-punch", label: "Zeit", icon: "⏱️" }, -    { to: "/payslips", label: "Lohn", icon: "📄" }, -    { to: "/personal-data", label: "Profil", icon: "👤" }, -]; - -const ADMIN_NAV = [ -    { to: "/admin/dashboard", label: "Admin", icon: "📊" }, -    { to: "/admin/projects", label: "Projekte", icon: "🗂️" }, -    { to: "/admin/users", label: "Team", icon: "👥" }, -    { to: "/admin/analytics", label: "Reports", icon: "📈" }, -]; - -const USER_PATHS = USER_NAV.map((item) => item.to); -  function MobileTabBar() {      const location = useLocation(); +    const { currentUser } = useAuth();      const { pathname } = location;   +    const featureKeys = useMemo(() => { +        const raw = currentUser?.companyFeatureKeys; +        if (Array.isArray(raw)) { +            return new Set(raw); +        } +        if (raw && typeof raw === "object") { +            return new Set(Object.values(raw)); +        } +        return new Set(); +    }, [currentUser?.companyFeatureKeys]); + +    const hasRole = (role) => currentUser?.roles?.includes(role); +    const hasFeature = (featureKey) => +        hasRole("ROLE_SUPERADMIN") || !featureKey || featureKeys.has(featureKey); + +    const userNav = useMemo(() => { +        const items = [{ to: "/dashboard", label: "Start", icon: "🏠" }]; +        if (currentUser?.isPercentage) { +            items.push({ to: "/percentage-punch", label: "Zeit", icon: "⏱️" }); +        } +        if (hasFeature("payroll")) { +            items.push({ to: "/payslips", label: "Lohn", icon: "📄" }); +        } +        items.push({ to: "/personal-data", label: "Profil", icon: "👤" }); +        return items; +    }, [currentUser?.isPercentage, currentUser?.roles, featureKeys]); + +    const adminNav = useMemo(() => { +        const items = [{ to: "/admin/dashboard", label: "Admin", icon: "📊" }]; +        if (currentUser?.customerTrackingEnabled && hasFeature("projects")) { +            items.push({ to: "/admin/projects", label: "Projekte", icon: "🗂️" }); +        } +        items.push({ to: "/admin/users", label: "Team", icon: "👥" }); +        if (hasFeature("analytics")) { +            items.push({ to: "/admin/analytics", label: "Reports", icon: "📈" }); +        } +        if (hasFeature("payroll") && (hasRole("ROLE_ADMIN") || hasRole("ROLE_PAYROLL_ADMIN"))) { +            items.push({ to: "/admin/payslips", label: "Lohn", icon: "📄" }); +        } +        return items; +    }, [currentUser?.customerTrackingEnabled, currentUser?.roles, featureKeys]); + +    const userPaths = userNav.map((item) => item.to);      const inAdminArea = pathname.startsWith("/admin"); -    const inUserArea = USER_PATHS.some((path) => pathname === path || pathname.startsWith(`${path}/`)); +    const inUserArea = userPaths.some((path) => pathname === path || pathname.startsWith(`${path}/`));        if (!inAdminArea && !inUserArea) {          return null;      }   -    const navItems = inAdminArea ? ADMIN_NAV : USER_NAV; +    const navItems = inAdminArea ? adminNav : userNav; + +    if (navItems.length === 0) { +        return null; +    }        return (          <> diff --git a/Chrono-frontend/src/components/VacationCalendar.jsx b/Chrono-frontend/src/components/VacationCalendar.jsx index 309c0e42..74189bb3 100644 --- a/Chrono-frontend/src/components/VacationCalendar.jsx +++ b/Chrono-frontend/src/components/VacationCalendar.jsx @@ -24,6 +24,36 @@ function formatLocalDateYMD(d) {      return `${year}-${month}-${day}`;  }   +function countChargeableDaysInRange(startDate, endDate, holidaysMap, halfDay = false) { +    if (!startDate || !endDate) { +        return 0; +    } + +    const start = new Date(`${startDate}T00:00:00`); +    const end = new Date(`${endDate}T00:00:00`); +    if (Number.isNaN(start.getTime()) || Number.isNaN(end.getTime()) || end < start) { +        return 0; +    } + +    let days = 0; +    for (let d = new Date(start); d <= end; d.setDate(d.getDate() + 1)) { +        const dateStr = formatLocalDateYMD(d); +        const dayOfWeek = d.getDay(); +        const isWeekend = dayOfWeek === 0 || dayOfWeek === 6; +        const isHoliday = Boolean(holidaysMap?.[dateStr]); + +        if (!isWeekend && !isHoliday) { +            days += 1; +        } +    } + +    if (halfDay && startDate === endDate && days > 0) { +        return 0.5; +    } + +    return days; +} +    function VacationCalendar({ vacationRequests, userProfile, onRefreshVacations }) {      const { notify } = useNotification(); @@ -120,24 +150,7 @@ function VacationCalendar({ vacationRequests, userProfile, onRefreshVacations })          if (!vacs) return 0;          return vacs.reduce((sum, vac) => {              if (vac.usesOvertime) return sum; - -            const startDt = new Date(vac.startDate + "T00:00:00"); -            const endDt = new Date(vac.endDate + "T00:00:00"); - -            if (isNaN(startDt.getTime()) || isNaN(endDt.getTime())) return sum; - -            let daysInRequest = 0; -            for (let d = new Date(startDt); d <= endDt; d.setDate(d.getDate() + 1)) { -                const dateStr = formatLocalDateYMD(d); // Korrekte Formatierung für den Holiday-Check -                const dayOfWeek = d.getDay(); -                const isWeekend = dayOfWeek === 0 || dayOfWeek === 6; -                const isHoliday = holidays[dateStr]; - -                if (!isWeekend && !isHoliday) { -                    daysInRequest += vac.halfDay ? 0.5 : 1.0; -                } -            } -            return sum + daysInRequest; +            return sum + countChargeableDaysInRange(vac.startDate, vac.endDate, holidays, vac.halfDay);          }, 0);      }   @@ -233,6 +246,25 @@ function VacationCalendar({ vacationRequests, userProfile, onRefreshVacations })              return notify(t('vacation.halfDayOneDay', 'Halbtags Urlaub kann nur für einen einzelnen Tag beantragt werden.'), 'error');          }   +        const requestedDays = countChargeableDaysInRange( +            vacationStartDate, +            vacationEndDate, +            holidays, +            vacationType === 'half' +        ); +        if (requestedDays <= 0) { +            return notify( +                t('vacation.noChargeableDays', 'Im gewählten Zeitraum liegt kein anrechenbarer Urlaubstag.'), +                'warning' +            ); +        } +        if (!useOvertime && requestedDays > remainingDays) { +            return notify( +                t('vacation.exceedsRemaining', 'Der beantragte Urlaub überschreitet deine verbleibenden Urlaubstage.'), +                'error' +            ); +        } +          let overtimeDeductionMinutesParam = null;          if (useOvertime && userProfile.isPercentage) {              if (!overtimeDeductionHours) { @@ -245,6 +277,13 @@ function VacationCalendar({ vacationRequests, userProfile, onRefreshVacations })                  return;              }              overtimeDeductionMinutesParam = Math.round(hours * 60); +            if (overtimeDeductionMinutesParam > (userProfile?.trackingBalanceInMinutes || 0)) { +                notify( +                    t('vacation.overtimeExceedsBalance', 'Die angegebene Überstundenmenge übersteigt deinen aktuellen Saldo.'), +                    'error' +                ); +                return; +            }          }            try { @@ -565,6 +604,7 @@ VacationCalendar.propTypes = {      userProfile: PropTypes.shape({          username: PropTypes.string.isRequired,          annualVacationDays: PropTypes.number, +        trackingBalanceInMinutes: PropTypes.number,          isPercentage: PropTypes.bool,          isHourly: PropTypes.bool,          company: PropTypes.shape({ cantonAbbreviation: PropTypes.string }), @@ -573,4 +613,4 @@ VacationCalendar.propTypes = {      onRefreshVacations: PropTypes.func  };   -export default VacationCalendar; \ No newline at end of file +export default VacationCalendar; diff --git a/Chrono-frontend/src/pages/HourlyDashboard/HourlyDashboard.jsx b/Chrono-frontend/src/pages/HourlyDashboard/HourlyDashboard.jsx index aa24d954..3ab5a54e 100644 --- a/Chrono-frontend/src/pages/HourlyDashboard/HourlyDashboard.jsx +++ b/Chrono-frontend/src/pages/HourlyDashboard/HourlyDashboard.jsx @@ -33,6 +33,8 @@ import PrintReportModal from '../../components/PrintReportModal.jsx';    import '../../styles/HourlyDashboardScoped.css';   +const getProjectCustomerId = (project) => String(project?.customer?.id ?? project?.customerId ?? ''); +  const HourlyDashboard = () => {      const { t } = useTranslation();      const { notify } = useNotification(); @@ -203,6 +205,30 @@ const assignCustomerForDay = async (isoDate, customerId) => {          }      }, [selectedProjectId]);   +    useEffect(() => { +        if (!selectedProjectId) { +            return; +        } + +        const selectedProject = projects.find((project) => String(project.id) === String(selectedProjectId)); +        if (!selectedProject) { +            setSelectedProjectId(''); +            setSelectedTaskId(''); +            return; +        } + +        if (selectedCustomerId && getProjectCustomerId(selectedProject) !== String(selectedCustomerId)) { +            setSelectedProjectId(''); +            setSelectedTaskId(''); +        } +    }, [projects, selectedCustomerId, selectedProjectId]); + +    useEffect(() => { +        if (selectedTaskId && !tasks.some((task) => String(task.id) === String(selectedTaskId))) { +            setSelectedTaskId(''); +        } +    }, [tasks, selectedTaskId]); +      useEffect(() => {          if (currentUser) {              fetchDataForUser(); @@ -306,6 +332,9 @@ const assignCustomerForDay = async (isoDate, customerId) => {      };        const weeklyTotalMins = dailySummaries.reduce((acc, curr) => acc + (curr.workedMinutes || 0), 0); +    const availableProjects = selectedCustomerId +        ? projects.filter((project) => getProjectCustomerId(project) === String(selectedCustomerId)) +        : projects;        if (!currentUser || !userProfile) {          return <div>Wird geladen...</div>; @@ -336,6 +365,7 @@ const assignCustomerForDay = async (isoDate, customerId) => {                  customers={customers}                  recentCustomers={recentCustomers}                  projects={projects} +                availableProjects={availableProjects}                  tasks={tasks}                  selectedCustomerId={selectedCustomerId}                  setSelectedCustomerId={setSelectedCustomerId} diff --git a/Chrono-frontend/src/pages/HourlyDashboard/HourlyWeekOverview.jsx b/Chrono-frontend/src/pages/HourlyDashboard/HourlyWeekOverview.jsx index 71133cd8..ddd525f0 100644 --- a/Chrono-frontend/src/pages/HourlyDashboard/HourlyWeekOverview.jsx +++ b/Chrono-frontend/src/pages/HourlyDashboard/HourlyWeekOverview.jsx @@ -31,6 +31,7 @@ const HourlyWeekOverview = ({                                  customers,                                  recentCustomers,                                  projects, +                                availableProjects,                                  tasks,                                  selectedCustomerId,                                  setSelectedCustomerId, @@ -138,7 +139,7 @@ const HourlyWeekOverview = ({                          </select>                          <select value={selectedProjectId} onChange={e => setSelectedProjectId(e.target.value)}>                              <option value="">{t('noProject','Kein Projekt')}</option> -                            {projects.map(p => ( +                            {availableProjects.map(p => (                                  <option key={p.id} value={p.id}>{p.name}</option>                              ))}                          </select> @@ -369,6 +370,7 @@ HourlyWeekOverview.propTypes = {      customers: PropTypes.array,      recentCustomers: PropTypes.array,      projects: PropTypes.array, +    availableProjects: PropTypes.array,      tasks: PropTypes.array,      selectedCustomerId: PropTypes.oneOfType([PropTypes.string, PropTypes.number]),      setSelectedCustomerId: PropTypes.func, diff --git a/Chrono-frontend/src/pages/LandingPage.jsx b/Chrono-frontend/src/pages/LandingPage.jsx index a3356670..aa0ca5a2 100644 --- a/Chrono-frontend/src/pages/LandingPage.jsx +++ b/Chrono-frontend/src/pages/LandingPage.jsx @@ -296,7 +296,7 @@ const LandingPage = () => {                                  )}                              </p>                          </div> -                        <Link className="lp-btn lp-primary" to="/preise"> +                        <Link className="lp-btn lp-primary" to="/register">                              {t("landing.pricing.cta", "Preise ansehen & Konfiguration starten")}                          </Link>                      </div> diff --git a/Chrono-frontend/src/pages/Login.jsx b/Chrono-frontend/src/pages/Login.jsx index b631972e..521a183d 100644 --- a/Chrono-frontend/src/pages/Login.jsx +++ b/Chrono-frontend/src/pages/Login.jsx @@ -43,7 +43,7 @@ const Login = () => {        const checkNfc = async () => {          try { -            const res = await fetch(`${import.meta.env.VITE_APIURL}/api/nfc/read/1`); +            const res = await fetch(`${import.meta.env.VITE_API_BASE_URL}/api/nfc/read/1`);              if (!res.ok) return;                const contentType = res.headers.get('content-type') || ''; diff --git a/Chrono-frontend/src/pages/PayslipsPage.jsx b/Chrono-frontend/src/pages/PayslipsPage.jsx index 825254d0..8966e155 100644 --- a/Chrono-frontend/src/pages/PayslipsPage.jsx +++ b/Chrono-frontend/src/pages/PayslipsPage.jsx @@ -12,7 +12,7 @@ import "../styles/UserPayslipsPageScoped.css"; // <— NEU  const PayslipsPage = () => {      const { t } = useTranslation();      const { currentUser } = useAuth(); -    const isAdmin = currentUser?.roles?.includes('ROLE_ADMIN'); +    const isAdmin = currentUser?.roles?.includes('ROLE_ADMIN') || currentUser?.roles?.includes('ROLE_PAYROLL_ADMIN');        // States...      const [pendingSlips, setPendingSlips] = useState([]); @@ -225,4 +225,4 @@ const PayslipsPage = () => {      );  };   -export default PayslipsPage; \ No newline at end of file +export default PayslipsPage; diff --git a/Chrono-frontend/src/pages/PercentageDashboard/PercentageDashboard.jsx b/Chrono-frontend/src/pages/PercentageDashboard/PercentageDashboard.jsx index 0b1d06a1..039d755a 100644 --- a/Chrono-frontend/src/pages/PercentageDashboard/PercentageDashboard.jsx +++ b/Chrono-frontend/src/pages/PercentageDashboard/PercentageDashboard.jsx @@ -35,6 +35,8 @@ import PrintReportModal from "../../components/PrintReportModal.jsx";  import '../../styles/PercentageDashboardScoped.css'; // <— NEU: spezifische Fixes für Percentage     +const getProjectCustomerId = (project) => String(project?.customer?.id ?? project?.customerId ?? ''); +  const PercentageDashboard = () => {      const { t } = useTranslation();      const { notify } = useNotification(); @@ -135,6 +137,30 @@ const PercentageDashboard = () => {          }      }, [selectedProjectId]);   +    useEffect(() => { +        if (!selectedProjectId) { +            return; +        } + +        const selectedProject = projects.find((project) => String(project.id) === String(selectedProjectId)); +        if (!selectedProject) { +            setSelectedProjectId(''); +            setSelectedTaskId(''); +            return; +        } + +        if (selectedCustomerId && getProjectCustomerId(selectedProject) !== String(selectedCustomerId)) { +            setSelectedProjectId(''); +            setSelectedTaskId(''); +        } +    }, [projects, selectedCustomerId, selectedProjectId]); + +    useEffect(() => { +        if (selectedTaskId && !tasks.some((task) => String(task.id) === String(selectedTaskId))) { +            setSelectedTaskId(''); +        } +    }, [tasks, selectedTaskId]); +      const fetchDataForUser = useCallback(async () => {          if (!userProfile?.username) return;          try { @@ -346,12 +372,16 @@ const PercentageDashboard = () => {      }, 0);      const weeklyDiff = weeklyWorked - weeklyExpected;      const overtimeBalanceStr = minutesToHHMM(userProfile?.trackingBalanceInMinutes || 0); +    const hasPayrollFeature = currentUser?.companyFeatureKeys?.includes('payroll') ?? false; +    const availableProjects = selectedCustomerId +        ? projects.filter((project) => getProjectCustomerId(project) === String(selectedCustomerId)) +        : projects;        if (!userProfile) {          return ( <div className="percentage-dashboard scoped-dashboard"><Navbar /><div className="skeleton-card"></div></div> );      }      if (!userProfile.isPercentage) { -        navigate("/user", { replace: true }); +        navigate("/dashboard", { replace: true });          return null;      }   @@ -373,9 +403,11 @@ const PercentageDashboard = () => {                          <button onClick={() => setPrintModalVisible(true)} className="button-primary">                              {t("printReportButton", "Bericht drucken")}                          </button> -                        <Link to="/payslips" className="button-primary-outline" style={{ marginLeft: '1rem' }}> -                            {t('payslips.title')} -                        </Link> +                        {hasPayrollFeature && ( +                            <Link to="/payslips" className="button-primary-outline" style={{ marginLeft: '1rem' }}> +                                {t('payslips.title')} +                            </Link> +                        )}                      </div>                  </header>   @@ -395,10 +427,12 @@ const PercentageDashboard = () => {                      customers={customers}                      recentCustomers={recentCustomers}                      projects={projects} +                    availableProjects={availableProjects}                      tasks={tasks}                      selectedCustomerId={selectedCustomerId}                      setSelectedCustomerId={setSelectedCustomerId}                      selectedProjectId={selectedProjectId} +                    setSelectedProjectId={setSelectedProjectId}                      selectedTaskId={selectedTaskId}                      setSelectedTaskId={setSelectedTaskId}                      vacationRequests={vacationRequests} @@ -458,4 +492,4 @@ const PercentageDashboard = () => {      );  };   -export default PercentageDashboard; \ No newline at end of file +export default PercentageDashboard; diff --git a/Chrono-frontend/src/pages/PercentageDashboard/PercentageWeekOverview.jsx b/Chrono-frontend/src/pages/PercentageDashboard/PercentageWeekOverview.jsx index e6c4b4aa..70b411a5 100644 --- a/Chrono-frontend/src/pages/PercentageDashboard/PercentageWeekOverview.jsx +++ b/Chrono-frontend/src/pages/PercentageDashboard/PercentageWeekOverview.jsx @@ -30,6 +30,7 @@ const PercentageWeekOverview = ({                                      customers,                                      recentCustomers,                                      projects, +                                    availableProjects,                                      tasks,                                      selectedCustomerId,                                      setSelectedCustomerId, @@ -82,7 +83,7 @@ const PercentageWeekOverview = ({                          </select>                          <select value={selectedProjectId} onChange={e => setSelectedProjectId(e.target.value)}>                              <option value="">{t('noProject','Kein Projekt')}</option> -                            {projects.map(p => (<option key={p.id} value={p.id}>{p.name}</option>))} +                            {availableProjects.map(p => (<option key={p.id} value={p.id}>{p.name}</option>))}                          </select>                          <select value={selectedTaskId} onChange={e => setSelectedTaskId(e.target.value)}>                              <option value="">{t('noTask','Keine Aufgabe')}</option> @@ -278,6 +279,7 @@ PercentageWeekOverview.propTypes = {      customers: PropTypes.array,      recentCustomers: PropTypes.array,      projects: PropTypes.array, +    availableProjects: PropTypes.array,      tasks: PropTypes.array,      selectedCustomerId: PropTypes.oneOfType([PropTypes.string, PropTypes.number]),      setSelectedCustomerId: PropTypes.func, diff --git a/Chrono-frontend/src/pages/UserDashboard/UserDashboard.jsx b/Chrono-frontend/src/pages/UserDashboard/UserDashboard.jsx index 6212672c..5d218dc6 100644 --- a/Chrono-frontend/src/pages/UserDashboard/UserDashboard.jsx +++ b/Chrono-frontend/src/pages/UserDashboard/UserDashboard.jsx @@ -43,6 +43,8 @@ import CustomerTimeAssignModal from '../../components/CustomerTimeAssignModal';  import HourlyDashboard from '../../pages/HourlyDashboard/HourlyDashboard.jsx';  import PercentageDashboard from '../../pages/PercentageDashboard/PercentageDashboard.jsx';   +const getProjectCustomerId = (project) => String(project?.customer?.id ?? project?.customerId ?? ''); +  function UserDashboard() {      const { currentUser, fetchCurrentUser } = useAuth();      const { notify } = useNotification(); @@ -190,6 +192,30 @@ function UserDashboard() {          }      }, [selectedProjectId]);   +    useEffect(() => { +        if (!selectedProjectId) { +            return; +        } + +        const selectedProject = projects.find((project) => String(project.id) === String(selectedProjectId)); +        if (!selectedProject) { +            setSelectedProjectId(''); +            setSelectedTaskId(''); +            return; +        } + +        if (selectedCustomerId && getProjectCustomerId(selectedProject) !== String(selectedCustomerId)) { +            setSelectedProjectId(''); +            setSelectedTaskId(''); +        } +    }, [projects, selectedCustomerId, selectedProjectId]); + +    useEffect(() => { +        if (selectedTaskId && !tasks.some((task) => String(task.id) === String(selectedTaskId))) { +            setSelectedTaskId(''); +        } +    }, [tasks, selectedTaskId]); +      const fetchHolidaysForUser = useCallback(async (year, cantonAbbreviation) => {          const cantonKey = cantonAbbreviation || 'GENERAL';          if (holidaysForUserCanton.year === year && holidaysForUserCanton.canton === cantonKey) { @@ -315,6 +341,9 @@ function UserDashboard() {          const reqDate = parseISO(req.requestDate);          return reqDate >= selectedMonday && reqDate < addDays(selectedMonday, 7);      })).slice().sort((a, b) => (parseISO(b.requestDate) || 0) - (parseISO(a.requestDate) || 0)); +    const filteredProjects = selectedCustomerId +        ? projects.filter((project) => getProjectCustomerId(project) === String(selectedCustomerId)) +        : projects;        function openCorrectionModalForDay(dateObj) {          const isoDate = formatLocalDate(dateObj); @@ -497,7 +526,7 @@ function UserDashboard() {                                  </select>                                  <select value={selectedProjectId} onChange={e => setSelectedProjectId(e.target.value)}>                                      <option value="">{t('noProject','Kein Projekt')}</option> -                                    {projects.map(p => <option key={p.id} value={p.id}>{p.name}</option>)} +                                    {filteredProjects.map(p => <option key={p.id} value={p.id}>{p.name}</option>)}                                  </select>                                  <select value={selectedTaskId} onChange={e => setSelectedTaskId(e.target.value)}>                                      <option value="">{t('noTask','Keine Aufgabe')}</option>